# 測定レポート（survey）

- 測定日時: 2026-08-01T09:00:00+09:00
- 対象環境: current-test（<http://localhost:8080>）
- 対象コミット: 4c1f88e
- 測定エージェント・モデル・エフォート・スキルバージョン: Claude Code / opus / high / replace-strategy 0.1.0

## 1. セマンティクス測定（role プローブ）

| ページ | 対象要素数 | named | ratio | (role, name) 重複 | 備考 |
|---|---|---|---|---|---|
| /plans | 28 | 24 | 0.86 | 1 | 計画一覧・行内の割当操作 |
| /assignments | 31 | 25 | 0.81 | 2 | 割当一覧・複数選択のツールバー |

- unnamed の代表例: アイコンのみのソートボタン、行頭のチェックボックス
- 判断: ロケータマッピング層は小規模（unnamed は 2 ページで 10 件）。HTML の id/name は変更してよい

## 2. DB 復元可否

- 結果: 復元可
- 手段: PostgreSQL コンテナのボリュームスナップショット
- 実測手順: スナップショット取得 → 1 行更新 → リストア → 変更消失を確認（通しで実施）
- 復元所要時間: 11 秒
- 判断: 書き込み側パリティスイートも実施可能

## 3. 現行コードの入手性

| 対象 | 入手性 | ソースか | 備考 |
|---|---|---|---|
| バックエンド | 一部 | ソース | ./current-app/server。`/plans` の入口クエリは読めるが、`/assignments` の入口クエリを含むモジュールは受領資産に無い |
| フロントエンド | あり | ソース | ./current-app/web |

- 判断: バックエンドは直接移植、フロントエンドはパリティスイート＋ベースライン駆動。`/assignments` 側は受領資産に欠けている範囲がある

## 4. 副作用の棚卸し

| 副作用 | 所有者（機能ページ/バッチ） | 検証対象か（対象／スコープ外→gaps） | 根拠（実測/コード/推定） |
|---|---|---|---|
| なし | - | - | - |

## 5. 既存テストの有無・密度・実装非依存性

- 有無・件数: あり・単体 9 件（HTTP・DOM 経由ではない）
- 実装非依存（HTTP・DOM 経由）で新旧両方に当てられるか: 不可
- 実行確認: 1 回流して全件 pass

## 未測定の項目

- `/assignments` の入口クエリ（受領資産に無く未入手）
