{
  "$comment": [
    "build-skill-eval-benchmark.test.js の検出能力を示す変異の一覧（Issue #421）。",
    "判定行を無効化する変異で狙ったテストが落ちることを scripts/check-mutation-proof.js が再実行する。"
  ],
  "test_file": "scripts/build-skill-eval-benchmark.test.js",
  "mutations": [
    {
      "id": "TEXT-KEY",
      "why": "判定をテキストではなく位置（index）で対応づける",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const expectations = assertions.map((text) => ({ text, ...verdicts.get(text) }));",
      "replace": "  const ordered = [...verdicts.values()];\n  const expectations = assertions.map((text, i) => ({ text, ...ordered[i] }));",
      "occurrences": 1,
      "expect_failing": ["集計（揃った iteration） 判定が並べ替わっていてもテキストで対応づける"]
    },
    {
      "id": "POSITIONAL",
      "why": "位置配列（オブジェクトでない判定）を受理する",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "    if (typeof item !== \"object\" || item === null || Array.isArray(item)) {\n      die(\n        `${where}: expectations[${i}] が {text, passed, evidence} のオブジェクトでない（位置配列は受理しない）`,\n      );\n    }\n",
      "replace": "",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 判定の位置配列（真偽値の並び）を受理しない",
        "受理しない入力（exit 2） 判定の位置配列（[passed, evidence] の組）を受理しない"
      ]
    },
    {
      "id": "TEXT-GUARD",
      "why": "判定側の assertion テキスト検査を外す（オブジェクト判定とは別のガード）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "    if (typeof text !== \"string\" || text.length === 0) {\n      die(`${where}: ${at} の assertion テキストが非空の文字列でない（位置配列は受理しない）`);\n    }\n",
      "replace": "",
      "occurrences": 1,
      "expect_failing": ["受理しない入力（exit 2） text を持たない判定を受理しない"]
    },
    {
      "id": "KEYSET",
      "why": "キー集合の突き合わせを外す（件数だけ見る）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const missing = assertions.filter((t) => !verdicts.has(t));\n  const extra = [...verdicts.keys()].filter((t) => !assertions.includes(t));\n  if (missing.length || extra.length) {\n    const parts = [];\n    if (missing.length)\n      parts.push(`判定の無い assertion ${missing.length} 件: ${missing.join(\" / \")}`);\n    if (extra.length) parts.push(`宣言に無い判定 ${extra.length} 件: ${extra.join(\" / \")}`);\n    die(`${gradingPath}: ${metaPath} とキー集合が一致しない。${parts.join(\"、\")}`);\n  }\n",
      "replace": "",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） キー集合が eval_metadata と違えば落とす（判定の無い assertion）",
        "受理しない入力（exit 2） キー集合が eval_metadata と違えば落とす（宣言に無い判定）"
      ]
    },
    {
      "id": "SUMMARY",
      "why": "採点内訳と summary の突き合わせを外す",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "    if (summary[key] !== want) {\n      die(`${gradingPath}: summary.${key}=${summary[key]} が採点内訳（${want}）と違う`);\n    }\n",
      "replace": "",
      "occurrences": 1,
      "expect_failing": ["受理しない入力（exit 2） summary の件数が採点内訳と食い違えば落とす"]
    },
    {
      "id": "RATE",
      "why": "pass_rate の突き合わせを外す（内訳と食い違う採点を通す）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (round(computedRate, digits) !== round(summary.pass_rate, digits)) {\n",
      "replace": "  if (false) {\n",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） summary の pass_rate が採点内訳と食い違えば落とす",
        "受理しない入力（exit 2） 桁を丸めても説明できない pass_rate は落とす"
      ]
    },
    {
      "id": "STDEV",
      "why": "母集団標準偏差（n で割る）へ変える",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const variance = values.reduce((a, v) => a + (v - m) ** 2, 0) / (values.length - 1);",
      "replace": "  const variance = values.reduce((a, v) => a + (v - m) ** 2, 0) / values.length;",
      "occurrences": 1,
      "expect_failing": ["集計（揃った iteration） run の並び・evals_run・統計・Delta を固定する"]
    },
    {
      "id": "ROUND",
      "why": "Python 互換の丸めを素朴な Math.round へ変える",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  return Number(result.toFixed(digits));",
      "replace": "  return Math.round(value * 10 ** digits) / 10 ** digits;",
      "occurrences": 1,
      "expect_failing": ["集計（揃った iteration） run の並び・evals_run・統計・Delta を固定する"]
    },
    {
      "id": "RPC",
      "why": "runs_per_configuration を 3 に決め打ちする",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const runsPerConfiguration = [...counts][0];",
      "replace": "  const runsPerConfiguration = 3;",
      "occurrences": 1,
      "expect_failing": [
        "集計（揃った iteration） run の並び・evals_run・統計・Delta を固定する",
        "受理しない入力（exit 2） 採点の無い run は既定で落とし、--ungraded skip で除外する"
      ]
    },
    {
      "id": "ORDER",
      "why": "eval の並びをディレクトリ名順から番号順へ変える",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const evalDirs = listDirs(opts.dir, \"eval-\");",
      "replace": "  const evalDirs = listDirs(opts.dir, \"eval-\").sort(\n    (a, b) => Number(a.slice(5)) - Number(b.slice(5)),\n  );",
      "occurrences": 1,
      "expect_failing": ["集計（揃った iteration） run の並び・evals_run・統計・Delta を固定する"]
    },
    {
      "id": "UNGRADED",
      "why": "採点の無い run を既定で黙って除外する",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const opts = { ungraded: \"fail\", force: false, stdout: false };",
      "replace": "  const opts = { ungraded: \"skip\", force: false, stdout: false };",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 採点の無い run は既定で落とし、--ungraded skip で除外する"
      ]
    },
    {
      "id": "EXECUTOR",
      "why": "executor が混ざった run の検査を外す",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (executorKeys.size > 1) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） executor / model が混ざっていれば落とす（母集団を分ける）"
      ]
    },
    {
      "id": "DELTA",
      "why": "Delta の桁を pass_rate も 1 桁にする",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "      pass_rate: signed(runSummary[a].pass_rate.mean - runSummary[b].pass_rate.mean, 2),",
      "replace": "      pass_rate: signed(runSummary[a].pass_rate.mean - runSummary[b].pass_rate.mean, 1),",
      "occurrences": 1,
      "expect_failing": ["集計（揃った iteration） run の並び・evals_run・統計・Delta を固定する"]
    },
    {
      "id": "EVIDENCE",
      "why": "根拠（evidence）の空を受理する",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "    if (typeof evidence !== \"string\" || evidence.length === 0) {\n      die(`${where}: ${at} の evidence が非空の文字列でない`);\n    }\n",
      "replace": "",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） evidence が空なら落とす（根拠の無い判定を集計しない）"
      ]
    },
    {
      "id": "TIMING",
      "why": "timing.json が無い run を 0 秒 / 0 トークンで埋める",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (!existsSync(timingPath)) die(`${runDir}: timing.json が無い（時間・トークンの正本）`);",
      "replace": "  const timingMissing = !existsSync(timingPath);",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） timing.json が無ければ落とす（時間・トークンを 0 で埋めない）"
      ]
    },
    {
      "id": "CONFIG-SHAPE",
      "why": "片側の configuration しか無い eval を混ぜて集計する（Delta の母集団がずれる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (shapes.size > 1) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） configuration が eval ごとに違えば落とす（Delta の母集団がずれる）"
      ]
    },
    {
      "id": "EVAL-ID-DIR",
      "why": "eval_id とディレクトリ名の食い違いを見ない（別の eval として数える）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (Number(named[1]) !== evalId) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": ["受理しない入力（exit 2） eval_id がディレクトリ名と食い違えば落とす"]
    },
    {
      "id": "EMPTY-PAIR",
      "why": "採点済み 0 件の eval × configuration を突き合わせから外す（--ungraded skip の穴）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const emptyPairs = [...perPair].filter(([, n]) => n === 0).map(([k]) => k);",
      "replace": "  const emptyPairs = [];",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 採点済み 0 件の eval × configuration は --ungraded skip でも落とす"
      ]
    },
    {
      "id": "ASSERTION-SET",
      "why": "run 間の assertion 集合の突き合わせを外す（分母の違う混合平均を通す）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "    if (byShape.size > 1) {",
      "replace": "    if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 同じ eval の run が違う assertion 集合を採点していれば落とす"
      ]
    },
    {
      "id": "RATE-DIGITS",
      "why": "pass_rate の比較桁を保存値に合わせず 4 桁固定へ戻す（丸めた採点を誤って落とす）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const digits = Math.min(Math.max(storedDigits, 1), 4);",
      "replace": "  const digits = 4;",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 保存された pass_rate の桁が粗くても内訳が合っていれば受理する",
        "受理しない入力（exit 2） 保存値と内訳がずれていても統計は内訳から作る"
      ]
    },
    {
      "id": "EXECUTOR-NAME",
      "why": "executor.name の必須検査を外す（記録が無い run を「揃っている」に倒す）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (missingExecutor.length > 0) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": ["受理しない入力（exit 2） timing.json に executor.name が無ければ落とす"]
    },
    {
      "id": "DERIVED-RATE",
      "why": "出力・統計へ採点内訳の導出値でなく保存値を載せる（集計が採点と食い違う）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "        pass_rate: computedRate,",
      "replace": "        pass_rate: summary.pass_rate,",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 保存された pass_rate の桁が粗くても内訳が合っていれば受理する",
        "受理しない入力（exit 2） 保存値と内訳がずれていても統計は内訳から作る"
      ]
    },
    {
      "id": "JSON-SHAPE",
      "why": "成果物の JSON の形を検査しない（切り詰めた成果物が exit 1 の stack trace になる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 成果物の JSON が \"文字列\" なら exit 2（stack trace にしない）",
        "受理しない入力（exit 2） 成果物の JSON が [] なら exit 2（stack trace にしない）",
        "受理しない入力（exit 2） 成果物の JSON が null なら exit 2（stack trace にしない）"
      ]
    },
    {
      "id": "NOTES-JSON",
      "why": "notes の JSON を成果物と同じ読み方（plain object 要求）に戻す（文字列配列が落ちる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "      ? parseNotesJson(path, raw)",
      "replace": "      ? readJson(path)",
      "occurrences": 1,
      "expect_failing": ["集計（揃った iteration） notes は JSON の文字列配列でも受け取れる"]
    },
    {
      "id": "UNKNOWN-CONFIG",
      "why": "未知の configuration ディレクトリの検査を外す（1 文字違いの成果物を黙って捨てる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (unknownDirs.length > 0) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） configuration に使えないディレクトリがあれば落とす"
      ]
    },
    {
      "id": "STRAY-RUN",
      "why": "run-<番号> でないディレクトリの検査を外す（run を黙って捨てる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "      if (strayRuns.length > 0) {",
      "replace": "      if (false) {",
      "occurrences": 1,
      "expect_failing": ["受理しない入力（exit 2） run-<番号> でないディレクトリがあれば落とす"]
    },
    {
      "id": "BOTH-FORMS",
      "why": "verdicts と expectations の併存を落とさない（expectations を黙って無視する）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (grading.verdicts !== undefined && grading.expectations !== undefined) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） verdicts と expectations が両方あれば落とす（正本を決められない）"
      ]
    },
    {
      "id": "TOKENS-NUMBER",
      "why": "total_tokens を 0 で埋める（計測できていない run をトークン 0 として混ぜる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  const tokens = timing.total_tokens;\n",
      "replace": "  const tokens = timing.total_tokens ?? 0;\n",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） total_tokens が数値でなければ落とす（0 で埋めない）"
      ]
    },
    {
      "id": "METRICS-FILE",
      "why": "metrics.json の不在を黙って 0 で埋める",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (!existsSync(metricsPath)) {",
      "replace": "  if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） metrics.json が無ければ落とし、null のキーは測れなかったとして 0 で記録する"
      ]
    },
    {
      "id": "METRICS-KEYS",
      "why": "metrics の値の型検査を外す（文字列の件数をそのまま集計へ流す）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "    if (value !== undefined && value !== null && typeof value !== \"number\") {",
      "replace": "    if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） metrics.json のキーが無ければ 0 として記録し、型違いは落とす"
      ]
    },
    {
      "id": "EVAL-DIR-SHAPE",
      "why": "eval ディレクトリ名の形の検査を外す（突き合わせを飛ばして重複に気づけない）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "  if (!named) {",
      "replace": "  if (false && !named) {",
      "occurrences": 1,
      "expect_failing": ["受理しない入力（exit 2） eval ディレクトリ名が形に合わなければ落とす"]
    },
    {
      "id": "DUP-RUNS",
      "why": "同じ run が 2 つのディレクトリから来ても落とさない（片方の eval が 2 倍の重みになる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "    if (seenRuns.has(key)) {",
      "replace": "    if (false) {",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） 同じ run が 2 つのディレクトリから来ていれば落とす"
      ]
    },
    {
      "id": "SYMLINK-DIRS",
      "why": "listDirs をリンクを辿らない判定へ戻す（リンクの eval が黙って落ちる）",
      "file": "scripts/build-skill-eval-benchmark.js",
      "find": "        return statSync(join(parent, name)).isDirectory();",
      "replace": "        return readdirSync(parent, { withFileTypes: true }).some((e) => e.name === name && e.isDirectory());",
      "occurrences": 1,
      "expect_failing": [
        "受理しない入力（exit 2） シンボリックリンクの eval ディレクトリも集計に入る"
      ]
    }
  ]
}
