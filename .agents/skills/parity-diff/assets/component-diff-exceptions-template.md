# 承認済みインスタンス例外の根拠

<!-- parity-diff が .replace/parity/<slug>/component-diff-exceptions.md として生成する。このファイルの形式の正本は parity-diff が定義する。 -->
<!-- 宛先を間違えないこと: ここは「承認済み（説明済み・許容）の例外の根拠」を置く。未検証領域は gaps.md、実行ごとの検出結果は diff.md（別ファイル・別目的）。 -->
<!-- gaps.md の「宣言できない構造差」節にあるものは正規化対象外＝未検証として毎回 diff.md へ転記されるため、承認済み例外の根拠を混ぜると未検証と区別できなくなる。 -->
<!-- 見出しは component-diff-exceptions.json の component_diff_exception_causes[].id と同一文字列にする（evidence がその見出しアンカーを指すため）。 -->
<!-- 各節・各値は例。実際の原因・観測条件・承認記録で置き換える。 -->

- 対象 slug: （features.md の slug）
- 最終更新: （ISO 8601）

## font-subset-weight600

<!-- 節見出し ＝ cause の id。component-diff-exceptions.json の evidence は `component-diff-exceptions.md#font-subset-weight600` を指す。 -->

- **識別ラベル（`reason` と同じ文言）**: フォントのサブセットビルド差による weight 600 のラスタライズ差
- **インスタンス件数**: （この原因を参照しているインスタンス数。JSON 側の件数と一致させる。件数は検証の弱さのシグナルなので畳まない）

### なぜ `component_diffs`（系統差 T）で宣言できないか

（例: 現・新で computed style の値が一致し画素だけ違うため、T の照合キー〈`property` / `current` / `new`〉になる値の差が無い。
系統的な原因でも 1 回の宣言にできないのは形式上の制約であり、インスタンス例外が唯一の置き場所）

### 原因調査の経緯（観測条件を併記する）

<!-- 手近な代表値 1 点の結果を全体に一般化しない。比較の相手は常に現行であり、新側の実験変種どうしの一致を結論にしない。 -->
<!-- 条件を書けない結論は結論として扱わず、その候補は未説明のまま残す（例外に書かない）。 -->

| 観測条件（要素・サイズ・ウェイト・状態） | 測ったもの | 結果 |
|---|---|---|
| （例: 見出し / 20px / weight 600 / default） | フォントの版（`head.fontRevision`）とヒンティング命令の有無 | （例: 版は一致、`prep` テーブルの有無が現・新で異なる） |
| （例: 本文 / 14px / weight 400 / default） | 同上 | （例: 差ゼロ。この条件では再現しない＝原因を weight 600 に限定できる根拠） |

### 許容の根拠と代替案の検討

（例: ヒンティング命令を持つビルドを配信できないか確認したが、配信物を選べないため源流で消せない。
`parity-replace` の theming 工程で縮められないことを確認済み）

### ユーザー承認

<!-- 承認は原因単位。1 回の承認で、そのとき覆ったインスタンスがまとめて確定する（インスタンスごとに承認を取り直さない）。 -->
<!-- 判断材料は原因を代表するインスタンスのもので足りる。ただし覆う件数 N と内訳は承認時に提示する（件数は検証の弱さのシグナルなので隠さない）。 -->
<!-- 承認後にこの原因のインスタンスが増えたら、参照を足すだけにせず増分の承認を 1 回取って行を追記する。 -->
<!-- 行の N の合計（累計）は上記「インスタンス件数」＝ JSON 側の cause 参照数と一致すること。ずれていれば未承認のインスタンスがあり、超過分は未説明として数える（収束させない）。 -->

| 承認日時（ISO 8601） | この承認で覆った件数 N | 承認時に提示した判断材料（代表インスタンス） | 備考 |
|---|---|---|---|
| （例: 2026-07-30T09:00:00Z） | （例: 7。この時点の cause 参照数） | （例: 代表 1 件の crop 対、観測条件の表、内訳〈ページ／状態／ビューポート〉、源流で消せない理由） | （例: 初回承認） |
| （例: 2026-08-03T02:10:00Z） | （例: +2。累計 9 ＝ JSON 側の cause 参照数） | （例: 増えた 2 件の内訳と、観測条件で同一原因だと確かめた記録） | （例: 増分の承認。JSON 側は cause 参照と approved_at をこの日時で足す） |
