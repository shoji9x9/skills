# 移行元の静的資産の棚卸しと方針決定（`.replace/assets.md`）

移行元が配信している**静的資産**（画像・アイコン・favicon・ロゴ・図・本文の書体・アイコン用の書体）を、**新側へ写すかどうか**を実装が始まる前に種類ごとに一括で決める工程と記録の正本。
`replace-strategy setup` の手順 11 が台帳を作り、`parity-replace` / `parity-component` が実装時に読む。

**依存（`.replace/dependencies.md`）とは別物である。** 依存は「自前で書くか／どのパッケージを使うか」で、判断材料は更新頻度・採用規模・ライセンス・サイズ（[`dependency-selection.md`](dependency-selection.md)）。
資産は移行元の配信物そのもので、判断材料は**再配布の可否・出どころ・同等物で置き換えたときに残る差**になる。混ぜると次の失敗が起きる。

- 資産の判断が工程に無いまま、各機能の実装者が**実装順に 1 人ずつ**決める。同じ種類の資産に逆向きの判断が付き、後から覆る
- 依存選定の表が「本文フォント」の行しか持たないため、**アイコン用の書体が「依存」にも「画像」にも分類されない**
- 覆ったときに「何を根拠に取り込んだか」が残らず、次に同じ資産へ出会った人が同じ判断からやり直す

**比較の射程（[`scope.md`](scope.md)「比較・検証の範囲」）とは別の軸である。** `url()` 参照資産の中身が比較の対象外でも、**写さなければ画面に出ない**。本工程は「写すか」だけを決め、比較するかは変えない。

## 棚卸し（起点は 2 つ）

**どちらか一方だけでは取りこぼす。** 両方を突き合わせて 1 つの一覧にする。

1. **配信物・受領資産の中のファイル**: 現行ソース・ビルド成果物・受領資産（`current.received_assets`）の中の画像・書体ファイル。
   読めない（ソース非入手等）なら「未確認」と書き、2 だけで作った一覧を網羅とみなさない
2. **現行画面が実際に描いているもの**: 同梱の [`../scripts/asset-probe.mjs`](../scripts/asset-probe.mjs) の `assetProbe` 関数リテラルを chrome-devtools MCP の `evaluate_script` の `function` 引数に渡す
   （IIFE 化は不要。呼び方は `role-probe.mjs` と同じ）。
   対象ページは手順 9 のページ一覧から選び、**選んだページを台帳に明記する**（全ページでなくてよいが、ダイアログ・ヘッダー／フッターの共通部分を含める）

プローブの返り値の読み方:

| キー | 読むもの |
|---|---|
| `images` | `img` / `input[type=image]` / SVG の `image`・`use` の参照先と、`rendered`（描かれている件数） |
| `glyphs` | 疑似要素（`::before` / `::after`）の `content` の文字・コードポイント・`font-family`。**`privateUse: true` はアイコン用の書体のグリフの目印** |
| `urlRefs` | `background-image` 等の `url()` 参照（疑似要素を含む） |
| `fontFaces` / `loadedFonts` | `@font-face` の `src` と、実際に読み込まれた書体 |
| `icons` | favicon 等の `link[rel]`（`icon` / `apple-touch-icon` / `mask-icon`） |
| `manifests` | `link[rel=manifest]` の参照だけ（`iconsInspected: false`）。**プローブは manifest の `icons` を読まない**——manifest を開いて `icons` の各画像を棚卸しに加え、開けなければ「未確認」と書く（インストール用アイコンは描画で取得されず `resources` にも出ないことがある） |
| `resources` | 実際に取得された画像・書体ファイルの URL（`matchedBy` は `extension` / `initiator`）。拡張子だけで選り分けない |
| `unclassifiedResources` | 拡張子にも `initiatorType` にも当たらなかった取得（`/assets/content?id=123` のような拡張子なしの配信）。**捨てずに中身を確かめる**——クロスオリジンのシートが読めない書体はここにしか出ないことがある |
| `resourcesCompleteness` / `resourceEntryCount` | Resource Timing の網羅性は常に `unknown`（バッファ容量は API から読めず、`setResourceTimingBufferSize` で変わるため、溢れの有無を後から証明できない）。件数は目安として使い、**`resources` を「これで全部」の根拠にしない**——`fontFaces` / `urlRefs` / `unclassifiedResources` と突き合わせ、埋まらなければ「未確認」と書く |
| `shadowRoots` | 走査した open な shadow root の数（内側の要素・スタイルシートも上の各キーに含む）。closed な shadow root は読めないため、Web Components を使うページでは画面と突き合わせる |
| `unreadableSheets` | `cssRules` を読めなかったスタイルシート。**1 件以上なら `fontFaces` は網羅ではない**——`resources` の書体ファイルと突き合わせ、埋まらなければ「未確認」と書く |

- **`rendered: 0` の `img` を「画面に出ないから写さない」と決めない。** 同じ宿主・近傍の要素が疑似要素のグリフや `background-image` で**同じ場所を描いている**ことがある（`img` を隠してアイコン用の書体で描く実装）。
  `glyphs` / `urlRefs` の `samples[].path` と突き合わせ、実際に描いているものを行にする
- **書体は `font-family` と `fontFaces` の `src` で、本文用とアイコン用に分ける。** 同じ「移行元が配信している書体ファイル」でも判断が分かれることが実績で分かっているため、**別の行**にする
- 状態（ホバー・展開・エラー表示）でしか出ない資産は、その状態に入れてからもう一度プローブを当てる。入れなかった状態は台帳に「未走査」と書く

## 決めること（種類ごとに 3 択）

**行の単位は資産の種類**（例: 本文の書体、アイコン用の書体、ロゴ、画面内の図、favicon、状態アイコン）。個々のファイルは行の中に列挙する。**同じ種類に別々の判断を付けない。**
分けたいなら、分ける理由を書いて種類を分ける。

| 方針 | 意味 | 追加で要ること |
|---|---|---|
| **実体を写す** | 移行元の配信物（再エンコードを含む）を新側に同梱する | **再配布の可否**の根拠（ライセンス・契約・先方の許諾）。確認できなければ選ばない |
| **同等物を作る** | 輪郭を図形にする・自前で描く・同等のパッケージに置き換える | **残る差**を書き、**この時点で意図的差異として宣言する**（下記）。パッケージに置き換えるなら、その採否は [`dependency-selection.md`](dependency-selection.md) の基準で決めて `.replace/dependencies.md` に記録する |
| **写さない** | 新側に置かない | 画面から消えることが仕様上許されるかの根拠。許されないなら選ばない |

- **再配布の可否・出どころ・残る差を推測で埋めない。** 確認できない項目は「未確認」と書き、**選ぶ方針が要る項目（上表「追加で要ること」）が未確認なら**その方針で確定させない（候補を添えてユーザーに確認する。例: 再配布の可否が未確認なら「実体を写す」は選べないが、残る差を確認できた「同等物を作る」は選べる）
- **決めるのは人である。** スキルは棚卸し結果・候補・残る差を示し、ユーザーの決定を記録する

### 「同等物を作る」は選んだ時点で宣言する

**ラスタライズの差は実装では消せない。** 書体のラスタライズ（副画素の平滑化が乗る）と、輪郭を図形にした描画は、形と位置が合っていても**縁が一致しない**ので差分器に毎回出る。
宣言が無いまま実装すると、その差は差分器の出力に**説明の付かない差**として現れ、人は「実装の誤りか、資産を写さなかった結果か」を差分を見てから調べ直すことになる。
着手前に宣言しておけば、`parity-diff` のトリアージが最初からその根拠で分類でき、同じ判断を機能ごとに繰り返さない。

**ただし宣言だけでは画素経路の差は消えない。** `intentional_diffs` は分類の根拠としては全経路に効くが、
画素経路の候補は**インスタンス単位の例外**（`.replace/parity/<slug>/component-diff-exceptions.json` の `property: pixel`）でしか吸収できず、
それを書くのは差分が出た後の `parity-diff` である（正本は `parity-diff` の `references/normalize.md`「レジストリの適用対象」と「画素経路の例外の適用」）。
**宣言は往復を無くすものではなく、承認の根拠を実装前に用意して分類を決定論的にするもの**として扱う。

- ユーザーが「同等物を作る」を選んだら、**残る差を 1 文の宣言にして、ユーザーの承認を得てから** `intentional_diffs.may_change` へ非破壊追記する（承認済みの方針を 1 回記録するだけ。書き手区分の正本は [`project-config.md`](project-config.md)「キーの書き手とライフサイクル」）。
  台帳の「宣言」列に同じ文言を写す（照合キー）
- **承認が得られないうちは `may_change` に書かない**（`pending` にも逃がさない——`pending` は実装中に見つけた差の置き場で、機能ごとの棚卸しに毎回出る）。方針を未決のまま残す

## 記録（`.replace/assets.md`）

テンプレート: [`../assets/assets-template.md`](../assets/assets-template.md)。**更新は非破壊**（既存の決定を書き換えない。覆すときは新しい決定を追記し、前の決定に覆した理由を書く）。

- **無ければ最初に書くスキルがテンプレートから作る**（本工程の導入前に `setup` を終えたプロジェクトには存在しない。`setup` の再実行を待たない）
- 方針が未決の行は方針列を空欄にし、理由（未確認の項目・判断待ち）を書く。**暫定の方針で埋めない**

## 実装時に台帳に無い資産に出会ったら（`parity-replace` / `parity-component`）

**機能・部品の中で決めない。台帳へ戻す。**

1. 台帳（`.replace/assets.md`。無ければテンプレートから作る）の同じ種類の行を探す。**あればその方針に従う**（別の判断を付けない）
2. 無ければ、プローブの返り値（どのページで・どう描いているか）を添えて**台帳に方針空欄の行を追記**し、候補（3 択と、それぞれの再配布の可否・残る差）を示してユーザーに確認する
3. 決まるまで、その資産に依存する実装単位は進めない。`porting.md` には台帳の行を指す 1 行だけを残す（`porting.md` に判断を書いて進まない）
4. 決まったら台帳に記録し、「同等物を作る」なら上記のとおり宣言してから実装する
