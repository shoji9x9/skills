# 機能インベントリ（features）

<!-- replace-strategy が生成・更新する。このファイルの形式（slug 規則を含む）の正本は replace-strategy が定義し、 -->
<!-- 下流スキル（golden-dataset / parity-suite / parity-replace / parity-diff）は slug をここから引く（自分で採番しない）。 -->
<!-- 各行は例。実際の機能・値で置き換える。 -->
<!-- 更新は非破壊。既存の行・ヘッダ項目・テンプレートに無い追記も、書き直しで落とさない（正本は replace-strategy スキルの「features.md の更新は非破壊」）。 -->

- 最終更新: （ISO 8601）
- ゴールデンデータセット Issue: 未起票

## その他の Issue（4 種以外）

<!-- 4 種（ゴールデンデータセット／横断 API／機能／バッチ）のどれにも当てはまらない Issue の置き場所。 -->
<!-- 4 種のいずれかに当てはまるものはここに書かず、必ずその表・ヘッダ項目に置く（ここを受け皿にして 4 種の分解を省かない）。 -->
<!-- slug はインベントリ全体で一意（機能・横断 API・バッチと同じ名前空間）。成果物ディレクトリは持たないが status モードが Issue 状態を報告する。 -->
<!-- 依存順は数値のほか「先頭」「ゴールデンデータセットより前」等の位置指定でよい。該当が無ければ節を消さず「なし」と書く（節ごと消すと status が「未導出」と報告する）。 -->
<!-- 下の例（新側スキーマの前倒し）は既定ではない。前倒しするかは利用側の判断であり、setup がこの行を自動で作らない（利用側が決めた場合の記録例）。 -->

| slug | 内容 | 依存順 | 4 種に当てはまらない理由 | 影響範囲 | Issue | 状態 |
|---|---|---|---|---|---|---|
| schema | 新側スキーマを先に作る | 先頭（ゴールデンデータセットより前） | 特定の機能・リソース・バッチに帰属せず全テーブルにまたがる | 全テーブル | 未起票 | - |

## 機能一覧

<!-- 機能行は画面内の表示セクションではなく、利用者目的・データ境界（所有テーブル群）・依存関係・副作用の所有者で分解する -->
<!-- 複数ページを持つ機能は 1 行にまとめる（ページ単位の分割は Issue 内のフェーズで行う） -->
<!-- 単一機能からしか使われない API は横断 API 表に載せず、この表の「新規実装 API」に含める -->
<!-- slug: ASCII kebab-case・インベントリ全体で一意。日本語機能名は短い英語に要約（例: 注文一覧＋注文詳細 → order） -->
<!-- 依存順: 横断 API → 依存の浅い機能 → 深い機能。副作用出力は所有者の行に記録する（対象／スコープ外の別も） -->
<!-- Issue 列: 未起票なら「未起票」、起票済みなら番号（#12 等）。状態列: open / closed / -（未起票） -->

| slug | 機能名 | 依存順 | ページ | 新規実装 API | 依存する横断 API（リソース slug） | テーブル | 副作用出力 | Issue | 状態 |
|---|---|---|---|---|---|---|---|---|---|
| order | 注文管理 | 2 | /orders, /orders/:id | GET /api/orders | user | orders, order_items | CSV 出力（対象）、確認メール（スコープ外） | 未起票 | - |
| notification-banner | お知らせバナー | 1 | /orders | GET /api/notices | - | notices | - | 未起票 | - |

## ページ一覧

<!-- 機能一覧の「ページ」列の逆引き。1 ページに複数機能が乗るなら全 slug を列挙する。 -->
<!-- parity-suite が「同じページに乗る他機能の在席チェック」の根拠に使う（未実装の機能はスキップ扱いで置き、実装後に parity-replace が外す）。 -->
<!-- 機能を追加・分割したらこの表も更新する（漏れると在席チェックの対象から外れる）。 -->

| ページ | パス | 乗る機能（slug） |
|---|---|---|
| 注文一覧 | /orders | order, notification-banner |
| 注文詳細 | /orders/:id | order |

## ページ要素の帰属

<!-- ページの可視要素のうち、どの機能行のセクションにも収まらないものと、その配置の所有者（slug）。 -->
<!-- 可視要素は現行アプリの実画面から列挙する（機能一覧の「ページ」列からの逆算で済ませない）。 -->
<!-- 扱い: 対象（挙動まで作る）／スコープ外（配置のみ・その先の挙動は作らない）。スコープ外でも場所を占める要素は誰かが配置しないと現行との差分になる。 -->
<!-- 所有者はページ一覧でそのページに乗る機能の slug から選ぶ（そのページに乗らない機能を書くと、置く場所＝フェーズが無い）。 -->
<!-- 同じ要素が複数ページに出るならページごとに 1 行ずつ書く（共通ヘッダ・フッタが典型）。 -->
<!-- 所有者が決まらない要素が残る場合は、行を落とさず所有者を空欄のまま残し、候補 slug を添えて着手前にユーザーへ確認する。 -->
<!-- 空欄を暫定値で埋めない。空欄のままでも起票は進められる（未確定であることが機能 Issue の本文と status モードの報告に載る）。 -->
<!-- 該当が無ければ節を消さず「なし」と書く（未確認と「確かめた結果、帰属の無い要素が無い」を書き分ける）。 -->

| ページ | 要素 | 配置の所有者（slug） | 扱い | 備考 |
|---|---|---|---|---|
| 注文一覧 | 外部会員システムへの登録ボタン |  | スコープ外（配置のみ） | 遷移先は現行と同じ URL。押下後の画面は比較対象外。**所有者未確定**（候補: order / notification-banner のどちらでも配置可能）——着手前に確定させる |
| 注文一覧 | 共通ヘッダのユーザー名表示 | order | 対象 | 横断 API `user` の応答を表示 |
| 注文詳細 | 共通ヘッダのユーザー名表示 | order | 対象 | 同じ要素でもページごとに 1 行（置くのはページ単位のフェーズ） |

## 横断 API（リソース単位）

<!-- 2 つ以上の機能から使われる API をリソース単位でグルーピング。Issue はリソース単位で 1 本 -->
<!-- 参照テーブル: この横断 API が読み書きするテーブル。機能一覧の「テーブル」列に出てこないテーブルも必ずここに書く -->
<!-- （横断 API からしか参照されないテーブルはこの列だけが置き場所であり、抜けると golden-dataset の対象から落ちる） -->
<!-- 読むテーブルが本当に無い場合だけ `-` と書く。空欄は「未調査」として下流で確認待ちになる -->

| slug | リソース | API | fan-out（利用機能 slug） | 参照テーブル | Issue | 状態 |
|---|---|---|---|---|---|---|
| user | ユーザー | GET /api/users, GET /api/users/:id | order, report | users, user_roles | 未起票 | - |

## バッチ

| slug | バッチ名 | 入力 | 比較する出力（DB 状態・生成ファイル） | 参照テーブル | Issue | 状態 |
|---|---|---|---|---|---|---|
| monthly-summary | 月次集計 | ゴールデンデータセット | summaries テーブル、集計 CSV | orders | 未起票 | - |
