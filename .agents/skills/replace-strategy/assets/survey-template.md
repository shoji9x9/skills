# 測定レポート（survey）

<!-- replace-strategy setup が生成する。このファイルの形式の正本は replace-strategy が定義する。 -->
<!-- すべて実測値。測れなかった項目は「未測定」と明記する（推測で埋めない）。 -->

- 測定日時: （ISO 8601）
- 対象環境: （現行テスト環境の URL）
- 対象コミット: （現行リポジトリのコミット SHA。コード入手不可ならその旨）
- 測定エージェント・モデル・エフォート・スキルバージョン: （値）

## 1. セマンティクス測定（role プローブ）

対象ページ（一覧系・フォーム系・ダイアログを含む 3 ページ以上）:

| ページ | 対象要素数 | named | ratio | (role, name) 重複 | 備考 |
|---|---|---|---|---|---|
| /orders（例） | | | | | |

- unnamed の代表例（マッピング層の見積もり材料）: <プローブの unnamedSamples から>
- 判断: ロケータマッピング層の規模感 / HTML の id/name を変更してよいか（意図的差異レジストリ `pending` の確定）

## 2. DB 復元可否

- 結果: <復元可 | 復元不可 | 未測定>
- 手段: <スナップショット・dump/restore・コンテナリセット等。手段は問わない>
- 実測手順: スナップショット取得 → 無害な変更 → リストア → 変更消失の確認（通しで実施した場合のみ「復元可」）
- 復元所要時間: <値>
- 判断: 書き込み側パリティスイートの強度（復元不可なら自己作成データ＋後始末へフォールバック）

## 3. 現行コードの入手性

| 対象 | 入手性 | ソースか | 備考 |
|---|---|---|---|
| バックエンド | <あり/なし> | | |
| フロントエンド | <あり/なし> | | |

- 判断: 非対称設計（バックエンド直接移植／フロントエンド別戦略）の可否

## 4. 副作用の棚卸し

| 副作用 | 所有者（機能ページ/バッチ） | 検証対象か（対象／スコープ外→gaps） | 根拠（実測/コード/推定） |
|---|---|---|---|
| メール送信（例） | | スコープ外（gaps へ） | |

## 5. 既存テストの有無・密度・実装非依存性

- 有無・件数: <値>
- 実装非依存（HTTP・DOM 経由）で新旧両方に当てられるか: <値>
- 実行確認: <1 回流した結果 | 未実施>

## 未測定の項目

<!-- 測れなかったものと理由。空にしない（無ければ「なし」と書く） -->
