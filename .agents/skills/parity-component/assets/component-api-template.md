# 引数の設計（component-api）

<!-- parity-component の capture が .replace/components/<slug>/component-api.md として生成し、build が判断を追記する。 -->
<!-- このファイルの形式の正本は parity-component が定義する。 -->
<!-- 残すのは「割り出しの結果どう判断したか」。axis-diff.mjs の出力そのものは axes.json に併存させ、ここへ貼り直さない。 -->
<!-- 各行は例。実際の軸・判断で置き換える。 -->

- 対象 slug: （`.replace/components.md` の slug）
- 部品: （呼び名）
- 採取したインスタンス: （id の一覧。2 件未満なら共通部品として先に作る対象にしない）
- 採取した状態: （状態名の一覧。全インスタンスで同じ集合）
- 割り出しの出力: （`axes.json` のパス）／ `axis-diff.mjs` の exit: （0 でなければ採取へ戻る）

## 引数（可変軸から起こしたもの）

| 引数 | 対応する軸（状態 / 軸名） | 値の集合 | 既定値 | 既定値の根拠 |
|---|---|---|---|---|
| （例: `variant`） | （default / background-color ほか） | （現行に在る値を全部。採取していない値を足さない） | （どれか） | （その値だったインスタンス id） |

- 値の集合は**採取物から列挙する**。「一般的なボタンなら danger もあるはず」で足さない（現行に無い見た目を発明することになる）

## 引数にしなかった可変軸

<!-- 引数にしなかった軸は全件残す。後から「なぜこの差を吸収したのか」を追えるようにするため。 -->

| 軸（状態 / 軸名） | 割れ方 | 扱い | 根拠 |
|---|---|---|---|
| （例: default / rect/width） | 文脈で決まる差 | 中身から決まる | （文言の長さに追従していることの実測） |
| （例: default / padding-left） | 現行の不整合 | ユーザー確認へ | （`intentional_diffs.pending` の該当項目） |
| （例: hover / color） | 未判別 | ユーザーへ上げる | （区別する理由を見つけられていない） |

- **未判別を「現行の不整合」に倒さない。** 用途の違いを見つけられていないだけのことがある

## 状態の落とし先

<!-- 採取した状態のうち、実装のどこにも落ちていないものが無いことを確かめる表。空欄を残さない。 -->

| 採取した状態 | 落とし先 | 備考 |
|---|---|---|
| （default） | 基本のスタイル規則 | |
| （hover） | スタイル規則（`:hover`） | 利用側が渡す状態ではないので引数にしない |
| （disabled） | 引数 `disabled` | 利用側が制御する状態 |

## 実装方式の決定

<!-- 擬似要素の有無・標準コントロールかどうかは、値の差より先に見る（実装方式そのものが決まるため）。 -->

- 標準コントロールを使っているか: （現行の実測。`select` / `checkbox` 等）
- `::before` / `::after` で描いている箇所: （軸 `::before/<present>` の割り出し結果）
- 新側で採る方式: （現行の方式を写す。変えるならユーザー確認の記録）

## 未確定・確認待ち

- （ユーザー確認へ回した差と、`intentional_diffs.pending` の該当項目）
- （採取できず判断を保留した軸と、`gaps.md` の該当箇所）
