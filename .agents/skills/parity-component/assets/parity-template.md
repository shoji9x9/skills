# 部品の照合記録（parity）

<!-- parity-component の build が .replace/components/<slug>/parity.md として生成する。 -->
<!-- このファイルの形式の正本は parity-component が定義する。追記は非破壊（前の照合記録・改修の記録を書き換えない）。 -->
<!-- 各行は例。実際の照合結果で置き換える。 -->

- 対象 slug: （`.replace/components.md` の slug）
- 新側 target: （`side: new` の target 名。証跡は環境別に `new/<target>/build-metadata.json`）
- 基準の採取条件: （`metadata.json` の `capture_conditions` を指す。ここへ転記しない）
- 採取ツール版の一致: （trait-capture / css-rules-capture の `VERSION` が `metadata.json` の記録値と一致したか。不一致なら比較へ進まず両側を採り直す）

## カタログ側の自己ノイズ

<!-- 現行のノイズ基準値をそのままカタログへ当てない（描画する要素の数が違えば揺れ方も違う）。 -->

| インスタンス | 状態 | 画素 | 特性 |
|---|---|---|---|
| （id） | （状態名） | （2 回撮った同士の差分量） | （差分件数） |

## 照合結果

| インスタンス | 状態 | 画素 | 特性 | 判定 |
|---|---|---|---|---|
| （id） | （状態名） | （差分量 / 基準値） | （差分件数） | （一致 / 要対応 / 許容 / 環境ノイズ） |

## 分類（差分が出た件だけ）

<!-- 1 件ずつ、現行側と新側の対を並べて分類する。まとめて「だいたい同じ」と判断しない。 -->

| # | インスタンス / 状態 | 差の内容 | 分類 | 根拠 |
|---|---|---|---|---|
| 1 | （id / 状態） | （何がどう違うか。値の対で） | 要対応 | （現行の採取値と新側の値） |
| 2 | （id / 状態） | （同上） | 許容 | （`intentional_diffs.keep` / `component_diffs` の該当項目。**宣言に紐づかない差を許容にしない**） |
| 3 | （id / 状態） | （同上） | 環境ノイズ | （そのインスタンス × 状態の `noise_baseline` との比較。別の組み合わせの基準値を流用しない） |

## 往復

| 反復 | 戻した件数 | 描画に効く変更を入れた範囲 | 結果 |
|---|---|---|---|
| 1 | （要対応の件数） | （変更したファイル・トークンの範囲） | （残った件数） |

- 同じ箇所で 3 回以上往復したら停止してユーザーに上げる（原因が実装ではなく採取物か分類の側にある可能性が高い）

## 収束の宣言

<!-- 収束と未検証を並べて示す。未検証を書いたまま「差分ゼロで完了」とだけ報告すると確認済みと読まれる。 -->

- 未説明差分: （0 件であること。許容は全件が宣言に紐づいていること）
- `verification_commands.full`: （実行したコマンドと結果）
- 見本の網羅: （比較の母集合の件数 ＝ 照合した件数。母集合＝ インスタンス × 状態 − 宣言済みの unreachable_states）
- **未検証として残るもの**: （`gaps.md` の該当箇所。`inaccessible` / `unresolved` / 到達できない状態 / 採取環境依存）
- **この収束が意味しないこと**: 画面に載せたときの差（親からの継承・周囲の余白・隣接要素との重なり）は見ていない。機能の収束は `parity-diff` が判定する

## 改修の記録（非破壊追記）

<!-- 機能の実装中に部品へ手を入れたときは、ここへ追記する（前の照合記録を書き換えない）。手順の正本は parity-component の references/amend.md。 -->

| 日付 | 改修の理由 | 切り分け（利用側 / 部品 / 採取の漏れ） | 足した引数と既定値 | 全見本の再照合結果 | 破壊的変更の判断 |
|---|---|---|---|---|---|
| （ISO 8601） | （どの機能の実装で何が起きたか） | （どれか） | （既定値は改修前の挙動） | （差分ゼロで示す。目視で確かめない） | （不要 / ユーザーが承認した内容） |
