# DB スキーマ・設定の復元根拠

<!-- current-environment-bootstrap が .replace/bootstrap/schema.md として生成する。形式の正本は current-environment-bootstrap が定義する。 -->
<!-- 項目ごとに「値」と「根拠」の両方を書く。片方でも欠ければ「不足」として扱い、既定値で補完しない。 -->
<!-- dataset_mode: static（DB なし）では本ファイルを「該当なし」とし、現行の静的データ形式を同じ粒度で記録する。 -->

- 復元日時: （ISO 8601）
- 復元経路: <受領 DDL そのまま / マイグレーション全適用 / ORM 生成 / 併用>
- 構築を実際に通したか: <通した（＝復元可） / 通していない（＝未確認）>

## 1. エンジンと実行時設定

| 項目 | 値 | 根拠（資産・ファイル・位置） | 区分（確定／不足） |
|---|---|---|---|
| DB エンジン種別 | | | |
| バージョン（メジャー.マイナー） | | | |
| タイムゾーン | | | |
| バージョン互換パラメータ | | | |
| SQL の解釈を変えるモード設定 | | | |
| 追加オプション・拡張・コンポーネント | | | |

## 2. 文字コードと照合順序（階層ごと）

<!-- DB 既定と個別カラムの設定は別物。階層ごとに確認する。 -->

<!-- 階層の名前と数はエンジンで違う。持たない階層は「該当なし」と書き、代わりに同じ効果を持つ設定を行として足す。 -->
<!-- セッション／接続レベル（接続文字列・初期化 SQL・ログオン時に走る処理）を必ず 1 行持つ——DB 既定を揃えても、ここで上書きされれば並び順は一致しない。 -->

| 階層 | 文字コード | 照合／ソート順 | 根拠 | 区分 |
|---|---|---|---|---|
| インスタンス | | | | |
| DB | | | | |
| スキーマ／ユーザー | | | | |
| テーブル（既定と例外） | | | | |
| カラム（既定と異なるもの） | | | | |
| セッション／接続（アプリの接続経路） | | | | |

## 3. 構造・制約・プログラム

| 分類 | 復元できた範囲 | 根拠 | 区分 |
|---|---|---|---|
| テーブル・カラム・型・NULL 可否・既定値 | | | |
| PK / FK / UNIQUE / CHECK / インデックス | | | |
| シーケンス（開始値・増分） | | | |
| ビュー | | | |
| トリガー | | | |
| 関数・プロシージャ | | | |

## 4. 初期化・マイグレーションの実行順

- 実行順の根拠: <マイグレーションのファイル名規則 / 履歴テーブル / 手順書>
- 適用済みとみなす範囲: <値>
- 手作業の前提を含むマイグレーション: <あれば列挙。無ければ「なし」>

## 5. 埋まらなかった領域

<!-- ORM 定義・マイグレーションが表現しない領域（照合順序・トリガー・プロシージャ・一部の CHECK 等）。 -->
<!-- 空にしない（無ければ「なし」と書く）。既定値で補完せず、不足として質問票へ回す。 -->

| 項目 | なぜ埋まらないか | 質問票の該当項目 | 未確定のまま進めた場合の影響 |
|---|---|---|---|

## 6. 構築ツール・手順

- ツールの所在: `<bootstrap_tool_dir>` 配下のパス
- エントリ: <空の DB からスキーマを構築する入口>
- 実行に要る環境変数の**名前**: <値は書かない>
