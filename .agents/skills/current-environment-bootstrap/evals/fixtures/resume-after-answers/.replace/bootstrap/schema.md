# DB スキーマ・設定の復元根拠

- 復元日時: 2026-08-18T04:20:00Z
- 復元経路: 受領 DDL そのまま
- 構築を実際に通したか: 通した（＝復元可）

## 1. エンジンと実行時設定

| 項目 | 値 | 根拠（資産・ファイル・位置） | 区分（確定／不足） |
|---|---|---|---|
| DB エンジン種別 | MySQL | `received/db/schema.sql` ヘッダ | 確定 |
| バージョン（メジャー.マイナー） | 8.0.36 | 同上 | 確定 |
| タイムゾーン | Asia/Tokyo | 同上 | 確定 |
| 互換モード・SQL モード | STRICT_TRANS_TABLES,NO_ENGINE_SUBSTITUTION | 同上 | 確定 |
| 拡張機能・プラグイン | なし | 同上に記載なし・DDL に依存なし | 確定 |

## 2. 文字コードと照合順序（階層ごと）

| 階層 | 文字コード | 照合順序 | 根拠 | 区分 |
|---|---|---|---|---|
| DB | utf8mb4 | utf8mb4_ja_0900_as_cs | `CREATE DATABASE` 句 | 確定 |
| スキーマ | 同上 | 同上 | 同上 | 確定 |
| テーブル（既定と例外） | utf8mb4 | utf8mb4_ja_0900_as_cs | 各 `CREATE TABLE` の末尾句 | 確定 |
| カラム（既定と異なるもの） | なし | なし | 各 DDL に個別指定なし | 確定 |

## 3. 構造・制約・プログラム

| 分類 | 復元できた範囲 | 根拠 | 区分 |
|---|---|---|---|
| テーブル・カラム・型・NULL 可否・既定値 | 全 3 テーブル | `received/db/schema.sql` | 確定 |
| PK / FK / UNIQUE / CHECK / インデックス | 全件 | 同上 | 確定 |
| シーケンス（開始値・増分） | AUTO_INCREMENT 既定 | 同上 | 確定 |
| ビュー | なし | 同上に定義なし | 確定 |
| トリガー | なし | 同上に定義なし | 確定 |
| 関数・プロシージャ | なし | 同上に定義なし | 確定 |

## 4. 初期化・マイグレーションの実行順

- 実行順の根拠: 単一の DDL のため順序なし
- 適用済みとみなす範囲: `received/db/schema.sql` 全体
- 手作業の前提を含むマイグレーション: なし

## 5. 埋まらなかった領域

| 項目 | なぜ埋まらないか | 質問票の該当項目 | 未確定のまま進めた場合の影響 |
|---|---|---|---|

なし

## 6. 構築ツール・手順

- ツールの所在: `bootstrap/`
- エントリ: `bootstrap/schema.sh`
- 実行に要る環境変数の**名前**: `MYSQL_DEFAULTS_FILE`
