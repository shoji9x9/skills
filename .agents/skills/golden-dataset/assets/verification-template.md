# 検証レポート（verification）

<!-- golden-dataset が .replace/dataset/verification.md として生成する。このファイルの形式の正本は golden-dataset が定義する。 -->
<!-- フェーズ A（現行側）とフェーズ B（新側・slug ごと）を分けて記録する。 -->
<!-- 各行は例。実際の検査結果で置き換える。 -->
<!-- dataset_mode: static では「テーブル」を静的データの単位、「外部キー整合・必須項目」をファイル間の参照整合・必須フィールドと読み替える。 -->

- version: （metadata.json の version と一致）
- affects: （この version で変更したテーブル／静的データ単位。metadata.json の changes と一致）
- mode: （db | static。metadata.json の mode と一致）
- 最終更新: （ISO 8601）

## フェーズ A（現行フェーズ）

### 整合性

<!-- FK 整合・必須項目・件数。冪等性（2 回実行して同一状態）の確認も残す。 -->

| 検査 | 結果 |
|---|---|
| 外部キー整合 | （例: 全 FK 参照先が存在） |
| 必須項目 | （例: NOT NULL 制約違反なし） |
| 件数 | （例: 期待件数と一致） |
| 冪等性（2 回実行で同一状態） | （例: 確認済み） |

### フィンガープリント（`dataset_mode: static` のみ）

<!-- metadata.json の current.fingerprint の算出方法を残す（値そのものは metadata.json が持つ）。db では本節を省く。 -->

| 項目 | 内容 |
|---|---|
| 対象 | （例: dataset_static_paths 配下の全生成物） |
| 算出方法 | （例: パスを昇順に並べ「相対パス + NUL + 内容」を連結して SHA-256） |
| 書き込み範囲の確認 | （例: 生成・削除の対象がすべて dataset_static_paths 配下に収まることを確認済み） |

### カバレッジ

<!-- どのテーブルの、どのパターンを含んだか。未カバーは design.md を参照。 -->

| テーブル | 含んだパターン |
|---|---|
| （テーブル名） | （例: null・境界値・空リスト用配置） |

### 検証コマンド（`verification_commands`）の適用

<!-- 投入ツールに通した検証コマンド（verification_commands.full）の結果。設定に無い場合・値がリスト（旧形式＝走る範囲が未宣言）の場合・ -->
<!-- 投入ツールのパス（dataset_tool_dir 配下）を対象に含まない場合も、「無い」「未宣言」「含まれていない」と書いて残す -->
<!-- （停止しないが、記録せずに進まない）。範囲を勝手に広げない。 -->

| 項目 | 内容 |
|---|---|
| `full` の有無 | （例: あり／設定に `verification_commands.full` が無いため未実行／値がリストで走る範囲が未宣言） |
| 投入ツールのパスを対象に含むか | （例: 含む／リント対象が新側アプリのソースに絞られており含まない） |
| 実行結果 | （例: 全コマンド成功／未実行） |

### 未投入（スコープ外＝未検証）

<!-- 投入できなかった論理データと理由。ストレージ実体への投入は v1 スコープ外（storage.seedable: true でも投入しない）。 -->
<!-- 「確認済みにしない」ため空にせず、無ければ「なし」と書く。ここに載せたものは parity-suite の gaps.md へも回す。 -->

| 対象 | 理由 | 影響（検証できない範囲） | gaps への記録 |
|---|---|---|---|
| （例: 添付ファイルの実体） | ストレージ投入はスコープ外（v1） | （例: 既存添付の一覧・サムネイル表示） | （例: 記録済み） |

### 意味論が未確定の機能（`current.origin: received-assets` のみ）

<!-- .replace/bootstrap/semantics.md の「確認待ち」「確認したが確定できなかったもの」のせいで、最低限のシナリオを確定できなかった機能。 -->
<!-- parity-suite がこの表を読んで開始可否を判断する。捏造で埋めて「確認済み」にしない。 -->
<!-- current.origin が managed・キー欠落なら「該当なし」と書く（空にしない）。 -->

| slug | 確定できない意味論 | 質問票の該当項目 | 確定するまでできないこと |
|---|---|---|---|
| （例: order-list） | （例: `orders.status` の正常系遷移） | （例: Q-3） | （例: この機能の parity-suite を開始できない） |

## フェーズ B（新側フェーズ）

<!-- slug ごとに 1 節。新側整合性／現新一致（差の列挙 × 宣言との完全一致）／検証の強度確認／保留を記録する。 -->

### slug: （features.md の slug）

- target: （投入先の新側 target 名）
- dataset_version: （新側へ投入した version）
- 検証日時: （ISO 8601）

#### 新側整合性

| 検査 | 結果 |
|---|---|
| 外部キー整合 | （例: 全 FK 参照先が存在） |
| 必須項目 | （例: 制約違反なし） |
| 件数 | （例: 現行と同数） |

#### 現新一致（差の列挙 × 宣言との完全一致）

<!-- 新側の実データと論理データで差のある箇所を列挙し、宣言済みの差分一覧（db_semantics / intentional_diffs.may_change）と完全一致するかで判定する。 -->
<!-- 逆写像（新側 → 論理）で往復させない（前方写像と同じ表を使う限り恒等になり、宣言外の正規化を足しても通る）。 -->
<!-- 説明できない不一致は失敗として扱い修正する（ここに残さない）。 -->

| 検出した差（対象） | 差の内容 | 宣言との対応（db_semantics / intentional_diffs.may_change の該当） |
|---|---|---|
| （例: orders.ordered_at） | （例: 現行は文字列 YYYY/MM/DD、新側は timestamp） | （例: db_semantics の日付型マッピング節） |
| （例: 一覧の並び順） | （例: collation 差で末尾 2 件が入れ替わる） | （例: db_semantics の collation 節） |

- 宣言外の差: （例: 0 件。1 件でもあれば失敗として修正し、修正後に再検証する）

#### 検証の強度確認

<!-- 宣言していない正規化を 1 件足して、検証が落ちることを確認する（落ちなければ検証が空回りしている）。 -->
<!-- 注入するのは実データに効くもの。何も抑え込まない正規化は列挙結果を変えず通ってしまい、健全な検証を「空回り」と誤判定する。 -->
<!-- 宣言済みの差を 1 件抑え込む正規化を選ぶか、選べなければ新側データへ宣言外の差を 1 件注入する（確認後に戻す）。 -->

| 注入したもの | 効くことの根拠 | 期待 | 結果 |
|---|---|---|---|
| （例: collation 差による並び順を揃えてから比較する） | （例: 宣言済みの「末尾 2 件の入れ替わり」を抑え込む） | 検証が失敗する | （例: 失敗を確認） |

#### 新側の未投入（スコープ外＝未検証）

<!-- 新側へ投入できなかったもの。ストレージ実体への投入は v1 スコープ外で、依存する検証は説明済みのデータ差として扱わない。 -->
<!-- 空にせず、無ければ「なし」と書く。 -->

| 対象 | 理由 | 影響（検証できない範囲） | gaps への記録 |
|---|---|---|---|
| （例: 添付ファイルの実体） | ストレージ投入はスコープ外（v1） | （例: 添付一覧の件数・サムネイル） | （例: 記録済み） |

#### 保留（pending）

<!-- 新規に見つかりレジストリ未登録の差。intentional_diffs.pending へ追記しユーザー確認へ回したもの。 -->

| 項目 | 差の内容 | 確認状況 |
|---|---|---|
| （例: 空文字と NULL の扱い） | （例: 現行は空文字、新側は NULL） | pending へ追記・ユーザー確認待ち |
