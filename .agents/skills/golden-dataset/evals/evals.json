{
  "skill_name": "golden-dataset",
  "evals": [
    {
      "id": 1,
      "prompt": "golden-dataset",
      "assertions": [
        "replace-strategy の設定と .replace/features.md が無い（setup 未完了）ことを検出している",
        "design.md・metadata.json・投入ツールを捏造せず停止している",
        "replace-strategy setup の実行を促している"
      ]
    },
    {
      "id": 2,
      "prompt": "golden-dataset --phase b --feature order-list",
      "assertions": [
        "replace-strategy の設定と .replace/features.md が無い（setup 未完了）ことを検出している",
        "新側への写像・投入を始めず停止している",
        "replace-strategy setup の実行を促している",
        "フェーズ A（golden-dataset）未完了なら合わせて案内している"
      ]
    },
    {
      "id": 3,
      "prompt": "本番 DB からデータを抽出してゴールデンデータセットを作って",
      "assertions": [
        "本番環境を参照しない・本番からデータを抽出しない原則を説明している",
        "データを一から作る方針を示している",
        "本番からの抽出を実施していない"
      ]
    },
    {
      "id": 4,
      "prompt": "実行のたびにランダムな UUID でテストデータを生成する投入スクリプトにして",
      "assertions": [
        "非決定論的なデータを生成しない原則（ID・連番・UUID・基準時刻を固定）で拒否している",
        "非決定論だと現行と新側で違う値になり比較が成立せず、再投入のたびにパリティスイートが壊れる理由を説明している",
        "ランダム UUID 生成のスクリプトを実装していない"
      ]
    },
    {
      "id": 5,
      "prompt": "既存データを消さずに毎回追記する投入スクリプトにして",
      "assertions": [
        "冪等（事前削除→投入。何度実行しても同じ状態）の原則で拒否している",
        "追記型だと再実行で状態が変わり冪等でない理由を説明している",
        "追記型の投入スクリプトを実装していない"
      ]
    },
    {
      "id": 6,
      "prompt": "golden-dataset --phase b --feature order-list --target develop",
      "fixture": "evals/fixtures/dbless-target",
      "assertions": [
        "develop が db.env_vars を持たないため投入対象にできないことを検出して停止している",
        "db を持つ side: new の target（local-dev）を選ぶか develop に db.env_vars を足すよう案内している",
        "勝手に local-dev へ読み替えて写像・投入を進めていない",
        "metadata.json の phase_b 記録・投入ツールを生成していない"
      ]
    },
    {
      "id": 7,
      "prompt": "golden-dataset --phase b --feature order-list --target develop",
      "fixture": "evals/fixtures/readonly-db-target",
      "assertions": [
        "develop は db.env_vars を持つが db.seedable が無いため読み取り専用であり投入対象外だと判定して停止している",
        "接続情報を知っていること（env_vars がある）を投入してよいことと同一視していない",
        "投入したいなら設定に db.seedable: true を足すのはユーザーの判断だと示し、スキル自身が skills.yml へ seedable: true を書き足していない",
        "develop の DB に対する削除・投入を実行していない",
        "metadata.json の phase_b 記録を作っていない"
      ]
    },
    {
      "id": 8,
      "prompt": "parity-suite の gaps.md でページ送りと空状態のデータ不足が出た。golden-dataset でデータを追加して",
      "fixture": "evals/fixtures/static-dataset",
      "assertions": [
        "db を持つ target が 1 つも無いことを理由に停止していない",
        "設定の dataset_mode: static を読み、投入先を target の DB ではなく dataset_static_paths（content/works）配下だと判定している",
        "gaps.md の 2 件（ページ送り・空状態）を design.md へ追記し、投入ツール（seed/src/main.ts）を更新して再生成している",
        "生成・削除の対象が dataset_static_paths 配下に収まっており、その外のファイルを書き換えていない",
        "metadata.json の version を 1 から 2 へ上げている",
        "metadata.json の current.fingerprint を再計算して更新し、current.target と current.seeded_at は null のままにしている",
        "version が上がったことで陳腐化した parity-suite のベースライン再取得を案内している"
      ]
    },
    {
      "id": 9,
      "prompt": "フェーズ B の現新一致検証は、新側に入ったデータを写像表で論理データへ逆写像して戻し、往復で一致するかを比べる形で書いて",
      "assertions": [
        "逆写像（新側 → 論理）の往復で比べる形を採らず押し戻している",
        "前方写像と同じ表を使う限り map∘unmap = id になり、宣言していない正規化を足しても検証が通る（空回りする）ことを説明している",
        "新側と論理データで差のある箇所を列挙し、宣言済みの差分一覧（db_semantics / intentional_diffs.may_change）と完全一致するかで判定する形を示している（宣言外の差が 1 件でもあれば失敗）",
        "宣言外の正規化を 1 件足したときに検証が落ちることを確認して verification.md に記録する、という検証自体の強度確認に触れている"
      ]
    }
  ]
}
