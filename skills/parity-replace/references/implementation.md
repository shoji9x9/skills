# 新側実装（現行コードを一次情報源に読む・確信度を申告する）

現行コードの読み方・照合単位の振り分け・確信度の常時申告・未解決の明示・振る舞い保存と品質改善の分離を定める。

## 現行コードは常にロジックの一次情報源

**現行コードはフロントエンド・バックエンドのいずれでも読む。** バリデーションルール・fetch・状態遷移・エラー処理・ログ送信は、コードを確認しないと再現できない。

非対称なのは「現行コードを読むか」ではなく**レビューの照合単位**である。

| | ロジックの情報源 | レビューの照合単位 |
|---|---|---|
| バックエンド（現行コードあり） | 現行コード | **旧新を並べた diff が成立する** |
| フロントエンド（1:1 対応が作れない） | 現行コード（ロジック）＋視覚ベースライン（見た目・構造） | **diff は成立しない。** パリティスイートが green か、`parity-diff` の差分がゼロか |

- `replace-strategy` の測定（現行コードの入手性）に従って振り分ける。現行コードが入手できない側はパリティスイートとベースラインだけが頼りになる
- **意図的差異レジストリの `keep`（テーブル名・項目名・API エンドポイント・同じ機能なら同じ関数名・スコープの広い変数名）が diff レビューを可能にしている。** ディレクトリ構成・ファイル名・狭いスコープの変数名は変えてよいが、変えるほど照合コストが上がる

## 推測せず、確信度を常に申告する

- **推測で実装しない。** 判断できない箇所は未解決であることをコード上に明示し（`TODO` コメント等）、レビューへ回す。**間違ったコードより未解決の明示のほうがよい**
- **確信度（高／中／低）を実装単位（ファイル、またはページ内のエリア）ごとに 1 行、常に申告する**（`porting.md`）
  - **迷ったときだけにしない。** 迷ったときだけの申告にすると「迷っていることに気づかなかった」箇所が素通りする。常に書けば「低」が普通の申告になり、言い出すコストが下がり、レビュー側の優先順位付けにも使える
  - **「低」は「おそらく間違っている。レビューで現行を読み直せ」の意味**とする
- **長い言い訳コメントが付いている箇所は、コードが間違っているサインとして扱う**
- **実装中に部品が足りないと分かったら、そのまま自前で書き始めない。** 実行フローの「部品の洗い出しと依存の決定」と同じ基準で判断し、`.replace/dependencies.md` へ追記してから実装を続ける
  （基準の正本は `replace-strategy` の `references/dependency-selection.md`）。`porting.md` の該当実装単位にも「どの部品をどう決めたか」を一行残す

## DB の方言差は書く前に点検する

**現行 DB と新 DB は、同じ意味で書いたクエリでも同じ結果を返さない。** 差は「読んでから書く」なら出ないが、
書いてから敵対的レビュー（手順 7）で拾うと手戻りが最も高くつく——しかも**絞り込みが全件に化ける類の差は、
ゴールデンデータセットの件数が少ないとパリティスイートが green のまま残る**ため、レビューだけが最後の砦になる。

- **点検表は設定の `references.db_semantics` から引く。** 点検項目（NULL の並び順・暗黙の型変換と失敗時の値・照合順序・
  連結時の NULL・書式のロケール依存）と、点検表が網羅ではないことの正本は `replace-strategy` の
  `references/project-config.md`「DB 意味論」。**ここへ転記しない**
- **点検表を通したことを実測の代替にしない。** 列挙されていない差は点検表からは出てこない——
  実際の検証は `golden-dataset`（フェーズ B の一致検証）と `parity-suite`（並び順の特性化）が担う
- **未整備（キー欠落・空値・解決できないパス）でも停止しない。** ただし**方言差を推測で埋めない**——
  正本の点検項目を、現行 DB と新 DB それぞれの**一次ドキュメント**で確認する（記憶や「たぶん同じ」で埋めない）。
  正本を読めない場合（姉妹スキル `replace-strategy` が未導入など。`references.db_semantics` の未整備とは別の事由）は
  点検を省略せず、`gh skill install shoji9x9/skills replace-strategy` での導入を促して停止する（`SKILL.md`「前提」の未インストール時の扱いと同じ）
- **点検の単位は「このフェーズで書くクエリ・データアクセス」**。機能全体の DB 仕様を先に調べ切るのではなく、
  書く直前に該当項目だけを当てる（フェーズ順を崩さない）
- **結果は `porting.md` の「DB 方言差の点検結果」へ記録する。** 該当なしも「該当なし」と書く——
  空欄にすると「差が無い」と「誰も見ていない」が区別できない
- **差を実装で吸収しないと決めたら、それは意図的差異である。** 単独で「許容」に倒さず `intentional_diffs.pending` へ
  非破壊追記してユーザー確認へ回す（下記「振る舞い保存と品質改善の分離」と同じ扱い）
- **確認しきれなかった項目は `TODO` で未解決を明示し、確信度を「低」で申告する**（上記「推測せず、確信度を常に申告する」）

## 書き方は新側リポジトリの規約に従う

**移植するのは現行の「振る舞い」であって、現行の「書き方」ではない。** 現行コードを一次情報源に読むことと、現行のコーディングスタイルを持ち込むことは別である。

- **規約は設定の `references.coding_conventions` から引く。** 解決順（規約ドキュメント → 新側リポジトリの基底ドキュメント・リント設定 → 既存コード）と、新側が別リポジトリのときの扱いの正本は
  `replace-strategy` の `references/project-config.md`「コーディング規約」。未整備でも停止しないが、**推測で自分の流儀を持ち込まない**
- **規約が現行の書き方と衝突したら規約を採る**（命名・エラー処理・型の扱い・ファイル分割）。ただし**意図的差異レジストリの `keep`（テーブル名・項目名・API エンドポイント・関数名）が優先する**——`keep` は旧新 diff レビューを成立させるための規律であり、規約より上位に置く
- **規約に合わせた結果として振る舞いが変わるなら、それは意図的差異である。** 勝手に進めず `intentional_diffs.pending` へ非破壊追記してユーザー確認へ回す（下記「振る舞い保存と品質改善の分離」の適用例）
- 規約に従ったことは `verification_commands` の代替にならない（機械的な検査は別に通す。手順 7）。
  **逆に、規約のうち `verification_commands.full` で落ちない項目は機械検査が無い**——`setup` の仕分け結果
  （`.replace/strategy.md` の「未検証領域の扱い」。正本: `replace-strategy` の `references/project-config.md`「コーディング規約」）で
  未検査とされた項目は、敵対的レビュー（手順 7）で人が見る対象として `review.md` の「観点（機械検査が無いもの）」へ写し、レビュー役へ渡す。
  **`.replace/strategy.md` が無い／当該節が無い（本仕分けの導入前に `setup` を終えたプロジェクト）ときは停止せず**、その旨を同節に書いて進む——推測で未検査項目を作らず、規約ドキュメントを読んで観点を立てる

## 振る舞い保存と品質改善の分離

- **同じフェーズで両方を狙わない。** 忠実な移植は良い性質も悪い性質も等しく運ぶ前提で進め、レガシーの奇妙な挙動も再現する
- **リント起因の修正は許容する**（アクセシビリティ改善・不要な副作用の削除など）。**リントを off にしない**——ロケータマッピング層が現側の非セマンティックさを隔離しているため、新側を改善してもスイートは壊れない
- **改善によって role や構造が変わった場合は、意図的差異レジストリ（`intentional_diffs`）へ追記する。** `parity-diff` がそれをノイズフィルタとして使う
- **タブ順の厳密一致を目標にしない**（ARIA APG 準拠の実装で新の方が正しくてもタブ停止数が減りうる）
