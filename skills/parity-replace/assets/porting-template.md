# 移植メモ（porting）

<!-- parity-replace が .replace/parity/<slug>/porting.md として生成する。このファイルの形式の正本は parity-replace が定義する。 -->
<!-- 「推測しない」原則: 判断できない箇所は TODO でコード上に未解決を明示し、ここへ一覧化する。間違ったコードより未解決の明示のほうがよい。 -->
<!-- 確信度は迷ったときだけにしない。実装単位ごとに「常に」高／中／低を 1 行で申告する（低 = おそらく間違っている。レビューで現行を読み直せ）。 -->
<!-- 各行は例。実際の実装単位・箇所・理由で置き換える。 -->

- 対象 slug: （features.md の slug）
- モード: （feature | api-resource | batch）
- 最終更新: （ISO 8601）

## ページ分割とフェーズ進捗

<!-- feature モードのみ。api-resource / batch は「該当なし（画面系工程なし）」と書く。 -->

| ページ | フェーズ状態（未着手／実装中／新側 green／レビュー済） | 備考 |
|---|---|---|
| （ページ名） | （状態） | （読み取り→書き込みのどこか等） |

## 確信度の申告（実装単位ごと 1 行・常に書く）

<!-- 単位 = ファイル、またはページ内のエリア。迷ったときだけにしない。 -->

| 実装単位 | 確信度（高／中／低） | 根拠・懸念（低なら「現行のどこを読み直すか」） |
|---|---|---|
| （例: 一覧の取得ロジック） | 高 | 現行の fetch とバリデーションをそのまま移植 |
| （例: 状態遷移のエラー処理） | 低 | 現行の分岐を再現しきれていない可能性。current の該当関数を読み直す |

## DB 方言差の点検結果

<!-- クエリ・データアクセスを書く前に references.db_semantics の点検表（未整備なら replace-strategy の -->
<!-- references/project-config.md「DB 意味論」の点検項目）を当てた結果。該当なしも「該当なし」と書く。 -->
<!-- 空欄にすると「差が無い」と「誰も見ていない」が区別できない。 -->
<!-- dataset_mode: static のときは対象が静的データ形式（フィールド構成・エンコーディング・日付表記・区切り・ファイル分割）になる。 -->

| 点検項目 | 現行側の挙動 | 新側の挙動 | 差の有無と対応（実装で吸収／intentional_diffs.pending へ／該当なし／未確定） | 関係する実装単位 |
|---|---|---|---|---|
| NULL の並び順 | （例: 昇順で NULLS LAST） | （例: 昇順で NULLS FIRST） | 差あり → 実装で吸収（orderBy に NULLS 位置を明示） | 一覧の取得ロジック |
| 暗黙の型変換と失敗時の値 | （例: 変換エラーを既定値へ落とす） | （例: 変換エラーで例外） | 差あり → 実装で吸収（変換前に入力を検証） | 検索条件の解決 |
| 照合順序（collation） | （例: 大小・全角半角を畳む） | （例: 畳まない） | 差あり → 両辺に同じ正規化を当てる | 検索条件の解決 |
| 連結における NULL の扱い | （例: NULL を空文字として連結） | （例: NULL を伝播） | 差あり → 連結前に NULL を明示的に置換 | LIKE パターンの組み立て |
| 数値・日付の書式のロケール依存 | （例: セッション設定に依存） | （例: 固定） | 差あり → 変換にロケールを明示指定 | 数値条件の解決 |
| （点検表に固有の追加項目） | （現行側の既定） | （新側の既定） | （例: 該当なし） | （関係する実装単位） |

## 未解決として明示した箇所（TODO 一覧）

<!-- コード上の TODO と対応させる。未解決を隠さない。 -->

| 箇所（ファイル・関数） | 未解決の内容 | 対応（レビューで確認／要ユーザー確認） |
|---|---|---|
| （例: 送信処理の再試行） | 現行の再試行回数・間隔が読み取れない | current を読み直してから実装。レビューで確認 |

## 依存の決定（この機能で決めたもの）

<!-- 決定の正本は .replace/dependencies.md（判断材料込み）。ここには実装単位から辿れるよう 1 行だけ残す。 -->
<!-- 実装中に必要と分かったものも、自前で書き始める前に同じ基準で判断して dependencies.md へ追記する。 -->

| 部品（用途） | 決定（採用パッケージ／自前実装） | 決めた時期（実装前／実装中） | 関係する実装単位 |
|---|---|---|---|
| （例: 表コンポーネント） | （例: パッケージ採用） | 実装前 | 一覧ページ |

## 現側脆弱マッピングの不要化確認結果

<!-- parity-suite がマッピング層コメントに記録した脆弱箇所（div への CSS セレクタ等）が、新側で不要になったかを確認する。 -->

| 現側の脆弱箇所 | 新側で不要になったか | 記録（新側マッピングを書いたか・セマンティクス改善の有無） |
|---|---|---|
| （例: 保存ボタンの div セレクタ） | 不要になった | role=button で解決。新側マッピングは書かない |

## 差し戻し履歴（parity-diff との往復）

<!-- diff.md を入力に該当ページのフェーズから再開した記録。頭から作り直さない。 -->
<!-- 反復番号は target ごとに数えるため、どの target からの差し戻しかを必ず書く。 -->

| target | 反復 | 入力（diff.md の差分） | 再開したフェーズ | 対応 |
|---|---|---|---|---|
| （例: local-dev） | （例: 2） | （例: 一覧の余白差） | テーマ | component_diffs を宣言（ユーザー確認済み） |
