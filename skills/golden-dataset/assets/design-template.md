# データ設計（design）

<!-- golden-dataset が .replace/dataset/design.md として生成する。このファイルの形式の正本は golden-dataset が定義する。 -->
<!-- 「確認済みにしない」原則: 含めなかったパターンは理由付きで残す。代表性を「確認済み」と宣言しない。 -->
<!-- 各行は例。実際の対象テーブル・値・理由で置き換える。 -->
<!-- dataset_mode: static では「テーブル」を静的データの単位（ファイル／コレクション）、「FK・必須項目」をファイル間の参照・必須フィールドと読み替える。 -->

- version: （metadata.json の version と一致）
- affects: （この version で変更したテーブル／静的データ単位。metadata.json の changes と一致）
- base_time: （ISO 8601。時刻依存データの基準）
- 最終更新: （ISO 8601）

## 対象テーブル

<!-- .replace/features.md の対象テーブルと対応させる（推測で足さない）。 -->
<!-- features.md の機能／リソース: この表を読む slug を 3 表（機能一覧「テーブル」・横断 API / バッチ「参照テーブル」）から漏れなく写す。 -->
<!-- 　　写しが 1 slug 落ちると「どの機能もこの表を読まない」と読め、役割が「読み取りだけ」へ倒れて行数を増やす検討に入らない。 -->
<!-- 役割: 投入する ／ 読み取りだけ ／ FK 親のみ。消費側 slug が 1 つでも在れば「投入する」。JOIN で引くか FROM の母集合として引くかは役割を変えない。 -->
<!-- 　　FK 親のみ: どの機能も直接読まないが FK を満たすために投入が要る親表（消費側 slug は `-`。0 件だと子行の FK を満たせない）。 -->
<!-- 件数 0 / 1 は、それ自体が「踏めない」の印（下の「述語ごとの分岐被覆」で扱いを決める）。 -->
<!-- 写し漏れ・役割の矛盾・件数は scripts/predicate-coverage-check.mjs が features.md と突き合わせる。 -->

| テーブル | features.md の機能／リソース | 件数 | 役割 | 備考（FK 親・必須項目） |
|---|---|---|---|---|
| （テーブル名） | （slug） | （件数） | （投入する／読み取りだけ／FK 親のみ） | （依存関係・制約） |

## 含めたエッジケース

<!-- data-design.md のチェックリストと対応させる。含めたものを「どのテーブルのどの行で」示す。 -->

| エッジケース | 含めた／未カバー | どこで（テーブル・行） |
|---|---|---|
| null | 含めた | （例） |
| 境界値 | 含めた | （例） |
| 最大長 | 含めた | （例） |
| 権限・ロールのパターン | 含めた | （例） |
| 状態遷移の各状態 | 含めた | （例） |
| 空リストになるケース | 含めた | （例） |
| ページネーションの全操作を識別できる量 | 含めた | （例） |
| ソート順が判別できる並び | 含めた | （例） |

## 消費側パラメータ

<!-- 各機能がデータをどう絞り込み・並び替え・分割するかを現行実装／実測から記録し、必要な値と件数へ結び付ける。 -->

| 機能／リソース slug | テーブル | 絞り込み列・条件 | 並び替え列・方向・同値順 | ページサイズ／切替候補 | 根拠 |
|---|---|---|---|---|---|
| （slug） | （テーブル名） | （列・条件） | （列・ASC/DESC・tie-break） | （件数） | （対象版・採取時点を追跡できる観測／コード） |

## 述語ごとの分岐被覆

<!-- 「入れたものが入ったか」ではなく「入れたもので現行のどの分岐が踏めるか」を設計の段で数える表。 -->
<!-- 消費側パラメータの絞り込み列ごとに 1 行。真の側と偽の側の両方を踏める行数が入っているかを数える。 -->
<!-- 判定: 真の行数 ≥ 1 かつ 偽の行数 ≥ 1 なら「踏める」、どちらかが 0 なら「踏めない」。 -->
<!-- 　　0 件は真の分岐へ入れない。1 件（表そのものが 1 行）は絞り込みを外しても結果が変わらず、絞り込みが効いていることを観測できない。 -->
<!-- 扱い: 踏めない行だけ「足す」か「gaps に記録」を選ぶ（踏める行は `-`）。足すと version が上がり、交差する slug の採取物が陳腐化する。 -->
<!-- 　　判断材料は「その分岐を消費側が本当に使うか」。「gaps に記録」を選んだ行は根拠を空欄にしない。 -->
<!-- 述語 id は検証記録（verification.md の「述語ごとの該当行数」）と突き合わせる鍵。インベントリ全体で一意にする。 -->

| 述語 id | テーブル | 消費側 slug | 述語（列・条件） | 真の行数 | 偽の行数 | 判定 | 扱い | 根拠 |
|---|---|---|---|---:|---:|---|---|---|
| （例: orders-status-shipped） | （例: orders） | （例: order） | （例: status = 'shipped'） | （例: 3） | （例: 5） | 踏める | - | （例: 一覧の状態フィルタ。現行 /orders の SELECT を読了） |
| （例: roles-admin） | （例: roles） | （例: user） | （例: role = 'admin'） | （例: 1） | （例: 0） | 踏めない | gaps に記録 | （例: 現行 UI に管理者専用画面が無く、消費側がこの分岐を使わない） |

## ページネーション境界

<!-- 対象一覧ごとに UI の操作と 1 ページ表示件数から必要量を算出する。単に「2ページ分」で固定しない。 -->

| 対象一覧 | 確認した操作 | 表示件数 P | 必要ページ数 K | 採用件数 | 算出根拠・確認経路 |
|---|---|---:|---:|---:|---|
| （例: 注文一覧） | （例: 次／前、ページ番号、最終） | （例: 20） | （例: 3） | （例: 41） | （例: 最終ページへの直接遷移と中間ページを識別する最小件数。UI で操作を確認） |

## 未カバーの領域と理由

<!-- 含めなかったパターンを理由付きで残す（確認済みにしない）。parity-suite の探索で不足が判明したら追加する前提。 -->

| 未カバーの領域 | 理由 |
|---|---|
| （例: 特定ロールの権限境界） | （例: 対象機能外のため。判明したら追加） |

## 値の規律のメモ

<!-- 文字幅・桁数・改行など表示比較に影響する値の方針。テスト1 的な値ばかりにしない。 -->

- （例: 全角・半角・改行を含む現実的な値を混在させた）

## gaps 由来の追加履歴

<!-- parity-suite の gaps.md「データ不足」から戻って追加したパターンを version 付きで残す（versioning.md の戻りループ）。 -->

| version | affects | 追加したパターン | 由来（gaps の箇所） |
|---|---|---|---|
| （例: 2） | （例: orders） | （例: 一覧の空状態用データ） | （例: order の gaps.md データ不足行） |
