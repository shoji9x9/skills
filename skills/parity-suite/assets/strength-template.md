# 強度レポート（strength）

<!-- parity-suite が .replace/parity/<slug>/strength.md として生成する。このファイルの形式の正本は parity-suite が定義する。 -->
<!-- 故障注入（ネガティブコントロール）の結果を記録する。緑を「スイートは強い」と宣言しない（カタログ外は射程外）。 -->
<!-- 各行は例。実際の故障・注入手段・判定で置き換える。 -->

- 対象 slug: （features.md の slug）
- モード: （feature | api-resource | batch）
- 実施日時: （ISO 8601）
- スイートの assertion バージョン: （対象コミット SHA。assertion が変わるたびに再実行し、この値を更新する）
- 判定単位: 現行検出一式＝手書き assertion ＋ ベースライン ＋ 差分器（手書き assertion 単体では判定しない）

## 故障カタログ（既知の回帰分類から導出）

<!-- 恣意的に作らない。導出元 = 意図的差異レジストリ / DB 意味論の差 / コンポーネントライブラリ差 / 既知の移行ハザード -->

| 故障クラス | 具体的な故障 | 導出元 |
|---|---|---|
| 値・件数・並び（データ由来） | 並び順を逆転（例） | DB 意味論（collation 差） |
| 構造（aria） | role=alert を div に退化（例） | コンポーネントライブラリ差 |
| 静的ラベル・placeholder・role | ボタンラベルを改変（例） | 意図的差異レジストリ |
| 要素スタイル（色・余白・フォント・罫線） | 保存ボタンの色を変更（例） | 既知の移行ハザード |

## 注入結果

<!-- 注入手段: 上流摂動（page.route）/ DOM 摂動（page.evaluate）/ スタイル注入（addStyleTag 等・撮り直して差分器をベースライン相手に回す） -->
<!-- 検出 = 手書き assertion が赤 ∪ 差分器（特性照合／画素／aria 比較）が赤。すべて素通り = 一式の弱点。 -->

| 故障 | 故障クラス | 注入手段 | 手書き assertion 赤? | 差分器（特性照合）赤? | 差分器（画素）赤? | 差分器（aria 比較）赤? | 判定 |
|---|---|---|---|---|---|---|---|
| 並び順を逆転（例） | データ由来 | 上流摂動 | 赤 | - | - | - | 捕捉 |
| role=alert を div に退化（例） | 構造 | DOM 摂動 | 赤 | - | - | 赤 | 捕捉 |
| ボタン色を変更（例） | 要素スタイル | スタイル注入 | 緑 | 赤 | 赤 | - | 捕捉 |
| 罫線色を微変更（例） | 要素スタイル | スタイル注入 | 緑 | 緑 | 緑 | - | 素通り |

## 素通りした故障と対応

<!-- 素通り = 一式の弱点。強化（assertion 追加・差分器しきい値調整）するか、gaps へ送る。確認済みにしない。 -->

| 素通りした故障 | 対応（強化 / gaps 送り） | 備考 |
|---|---|---|
| 罫線色を微変更（例） | 強化: 特性照合の対象要素に追加（例） | ノイズ基準値に埋もれない量か再確認 |

## 差分器の健全性

<!-- 強度ゲートで確認し、検証済み差分器を parity-diff がそのまま再利用する（metadata.json 経由で引き渡す）。 -->

- 既知の差を赤にできるか: （確認結果）
- 注入量がノイズ基準値に埋もれないか: （確認結果）
- parity-diff へ引き渡す差分器（ツール・しきい値・ノイズ基準値の記録先）: metadata.json の `differ` / `noise_baseline`

## 未検証の故障種別

<!-- カタログの外は射程外＝強度の下限であって完全性の証明ではない。捕捉した種別と未検証の種別を分けて記録する。 -->

| 未検証の故障種別 | 理由 |
|---|---|
| （種別） | （カタログ外・実行環境の制約・データ不足など） |

## 再実行条件

- assertion が変わるたびに再実行する（穴埋め・データセット更新でベースライン再取得した場合を含む）。スケジュール実行ではない。
