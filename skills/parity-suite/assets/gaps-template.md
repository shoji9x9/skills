# 未検証領域（gaps）

<!-- parity-suite が .replace/parity/<slug>/gaps.md として生成する。このファイルの形式の正本は parity-suite が定義する。 -->
<!-- 「確認済みにしない」原則: ブラウザ／API で確認していない挙動を「確認済み」と書かない。理由付きで残す。 -->
<!-- 各行は例。実際の未検証箇所・理由で置き換える。 -->

- 対象 slug: （features.md の slug）
- 実施日時: （ISO 8601）

## 特性化できなかった箇所と理由

<!-- 種別: データ不足（→ golden-dataset へ戻す）/ 認証範囲不明 / スコープ外の副作用（メール・外部連携）/ ローディング indicator 無し 等 -->

| 箇所 | 種別 | 理由 | 対応（golden-dataset へ戻す／スコープ外として記録／要ユーザー確認） |
|---|---|---|---|
| 一覧の空状態（例） | データ不足 | このパターンのシードが無く空リストしか確認できない | golden-dataset へ戻してバージョンを上げ、影響ベースラインを再取得 |
| 確認メール送信（例） | スコープ外の副作用 | メール・外部連携はスコープ外 | 記録のみ（検証しない） |
| 一覧のローディング表示（例） | 明示的な loading UI 無し | indicator が無い画面は対象外 | 記録のみ |
| 管理者専用画面（例） | 認証範囲不明 | 認証が必要な範囲を特定できない | 未検証として残す（確認済みにしない） |

## hermetic でないテスト一覧

<!-- 後始末できない書き込みは、事前にユーザーへ残置可否を確認し、「hermetic でない」と明示して可視化する（隠さない）。 -->

| テスト | 残置データ | 承認記録（ユーザー確認の有無・日時） |
|---|---|---|
| 注文作成の書き込み特性化（例） | 一意プレフィックス付きデータが残置 | 承認済み（ISO 8601） |

## 強度ゲートで素通りした故障種別

<!-- strength.md の「素通り」と対応させる。強化しきれず未対応で残したものをここに一覧化する（確認済みにしない）。 -->

| 素通りした故障種別 | 理由・現状 |
|---|---|
| 罫線色の微差（例） | ノイズ基準値との分離が確認できず未対応 |

## 宣言できない構造差

<!-- このファイルの形式の正本は parity-suite（このファイル）が定義する。この節への追記者は現状 parity-replace（将来他スキルが構造 gap を追記する場合も、著者別に節を分けず本節へ集約する）。 -->
<!-- テーマでも component_diffs のクラス/トークン単位宣言でも表せない構造差（フォーカスリング形状・内部 DOM・余白の配り方等）を、parity-diff の正規化対象外＝未検証として残す。 -->
<!-- 各行は例。実際の箇所・内容・理由で置き換える。 -->

| 箇所 | 内容 | 理由（なぜ component_diffs で宣言できないか） |
|---|---|---|
| 保存ボタンのフォーカスリング（例） | 現側は角丸矩形、新側は楕円で形状が異なる | クラス/トークンのプロパティ差に還元できず、正規化ルールを書けない |
| カード内部の DOM 構造（例） | ラッパ要素の入れ子が新側で 1 段浅い | 余白の配り方が構造依存で、トークン差として表現できない |
