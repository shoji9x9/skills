# 依存パッケージの導入判断（自前実装かパッケージか・選定基準・導入タイミング）

新側実装で使う部品（UI コンポーネント・データ処理・フォント等）について、**自前で書くかパッケージを使うか**、使うならどれを選ぶかを決める工程と判断材料を定める。
**決定そのものはプロジェクトの裁量**であり、本ファイルが定めるのは「何を見て決めるか」と「いつ決めるか」。スキル群（`replace-strategy` / `golden-dataset` / `parity-suite` / `parity-replace` / `parity-diff` / `parity-component`）が依存を追加するときは、どのスキルでもこの基準に従う。

**アプリの骨格は本ファイルの対象外。** フレームワーク・レイヤ／ディレクトリ構成・API 設計方針・ホスティング構成は**事前定義でスキルは参照のみ**（正本: [`project-config.md`](project-config.md) の「新側アーキテクチャ」）。
本ファイルが扱うのは、その骨格の上に載せる**部品の採否**——スキルがこの基準で決めて `.replace/dependencies.md` に記録するもの——である。

**移行元が配信している画像・書体ファイルを写すかは本ファイルの対象外。** 判断材料（再配布の可否・出どころ・残る差）が違うため、正本は [`static-assets.md`](static-assets.md)、記録先は `.replace/assets.md`。
「本文フォント」の行をここに置いても、アイコン用の書体や画像を写すかは決まらない。資産を同等のパッケージで置き換えると決めた場合だけ、そのパッケージの採否を本ファイルの基準で決める。

避ける失敗は 2 つ。

- 既存のパッケージを検討せず、自前で複雑な実装を始める（例: 表・日付処理・仮想スクロール）
- 素性や再現条件を確認しないまま導入し、実装がほぼ終わった段階で差し替えになる

## いつ決めるか

| 部品の種類 | 決める工程 | 担当スキル |
|---|---|---|
| プロジェクト共通（UI ライブラリ・フォント・状態管理・日付処理など複数機能で使うもの） | `setup` の機能インベントリの後 | `replace-strategy` |
| 共通部品を画面より先に作る方針での、共通部品の実装に要るもの（`setup` で決定済みのものは除く） | `build` の手順 3（部品の採否。実装に入る前） | `parity-component` |
| 機能固有（その機能でしか使わないもの） | 実装フェーズに入る前（ページ分割の後） | `parity-replace` |
| 実装中に必要と判明したもの | 気づいた時点（そのまま自前実装で進めない） | `parity-replace` |
| 検証側の道具立て（投入ツール・パリティスイート・差分器が使う依存） | その道具を書き始める前 | `golden-dataset` / `parity-suite` / `parity-diff` |

- **実装を始めてから部品を探さない。** 差し替えコストは実装が進むほど上がる
- **事前にすべてを洗い出せる前提を置かない。** 実装中に増えるのは正常であり、そのときも同じ基準・同じ記録先で判断する
- 決定は `.replace/dependencies.md` に記録する（テンプレート: [`../assets/dependencies-template.md`](../assets/dependencies-template.md)）。実装中に追加したものも同じファイルへ追記し、敵対的レビューの対象にする

### 洗い出しの網羅（共通 UI プリミティブ）

**「複数機能で使う部品」は基盤ライブラリの種類だけを指さない。**
上の表の例（UI ライブラリ・フォント・状態管理・日付処理）はどれも**ライブラリの種類**なので、
例だけを手がかりに洗い出すと、ライブラリの外側にある**個々の UI プリミティブが漏れる**
（実案件で、最初に挙がったのはデータグリッド・共通ヘッダー・ボタンの 3 種だけで、
指摘を受けてからセレクト・テキストフィールド・ページネーション・ラジオボタン・チェックボックス・
モーダルダイアログ・トースト通知が見つかった）。

**次の種類を 1 つずつ、現行アプリのページを実際に開いて確かめる**（候補の生成源は `setup` の手順 9 のページ一覧）。
ソースの grep だけで済ませない——ライブラリが描く部品はアプリのマークアップに現れない
（アプリ側に出てくるのはライブラリのコンポーネント名だけで、その内部の部品は出てこない。現行ソースを入手できない場合は grep 自体が使えない）。
**初期表示に出ない種類は、その状態を作ってから判定する**——確認ダイアログ・トースト・バリデーションエラー・読み込み中・空状態・ツールチップは
ページを開いただけでは出ない。**状態を作る操作は、選んだ現行 target の `forbidden_actions` を確かめてから行う**——
`forbidden_actions` が**未定義の target は読み取り専用**、空リストは全て実施可で、適用対象はアプリへの UI / API 操作である
（正本は [`project-config.md`](project-config.md)「`forbidden_actions` の適用範囲」）。
**書き込みを伴わない手段を先に使う**: 絞り込みで 0 件にする（空状態）、不正値を入れて送信前の検証を出す（バリデーションエラー）、
要素にポインタを載せる（ツールチップ）。
**破壊的な操作の確認ダイアログは「開いて閉じるだけ」でも書き込み無しに数えない**——開くには削除ボタンを押す必要があり、
誤操作でそのまま実行に至る。その target でその操作が許可されているときだけ開く（破壊的でない確認ダイアログがあればそれを先に使う）。
保存・削除の実行が要るのは（トースト等）それ以外に状態を作れない場合だけで、**その操作が許可されている target に限る**。
**`--autonomous` の実行では書き込みを伴う手段を使わない**——現行環境への破壊的変更の可否は人の承認が要る判断で、
`forbidden_actions: []` は「人が許可した」ではなく「設定上は実施可」でしかない。
**確かめられなかった種類は台帳へ `未確認` の行として残す**（下記「洗い出しの 6 値」）——
禁止されている操作でしか作れない種類・状態を作れなかった種類を **`該当なし` と書かない**（ページを開いただけの不在は
「無い」ではなく「まだ見ていない」で、`該当なし` に倒すと共通部品が台帳から消える）。
理由と、確かめるのに要る条件（禁止された操作名を含む）を「理由・引き取り手」列に書き、あわせてユーザーへ報告する。
現行環境は比較の基準なので、状態を作るために保護されたデータを書き換えない。
**自律実行でも `未確認` は保留（`pending_decisions`）に落とさない**——成果物の値として残せる事実の記録であり、
引き取り手（その部品が要る機能の `parity-replace`）が別にあるため、`setup` はこの種類を残したまま完了できる。
区分の根拠は [`autonomy.md`](autonomy.md)「保留に落とすか、成果物の値として残すか」。

| 区分 | 種類 |
|---|---|
| 入力 | テキストフィールド（1 行・複数行・検索ボックス）、セレクト・コンボボックス、ラジオボタン、チェックボックス、日付・時刻の入力、数値・金額の入力、ファイル選択 |
| 操作 | ボタン（主・副・破壊的・アイコンのみ）、リンク、トグル・スイッチ、タブ |
| 表示 | テーブル・データグリッド、ページネーション、ラベル・バッジ・ステータス表示、空状態の表示、読み込み中の表示 |
| 通知・割り込み | モーダルダイアログ（確認・フォーム）、トースト・フィードバック通知、フォームのバリデーションエラー表示、ツールチップ |
| 骨組み（画面に共通して現れる枠。冒頭の「アプリの骨格」＝フレームワーク・構成とは別） | ヘッダー・グローバルナビゲーション、パンくず、フォームのレイアウト |

- **1 機能でしか使わないと確かめた種類は、この工程で採否を決めない**（`parity-replace` が実装フェーズ前に同じ基準で決める。上記「いつ決めるか」の「機能固有」の行）。
  `該当なし` にも採否にも倒さず **`機能固有` の行として残す**——`該当なし` は共通部品でないものを台帳から消し、採否は 1 機能ぶんの材料で全機能ぶんの決定をしたことになる
- **「無い」と書けるのは開いて確かめたときだけ。** 該当が無いと確かめた種類も `.replace/dependencies.md` に `該当なし` と残す——
  行が無いと「まだ見ていない」と読めるため、**確かめた結果としての不在**を台帳に残す
- **どの種類にも行を作る。** 確かめた結果が採否に至らなくても（内蔵・機能固有・未確認）値として残すので、
  **台帳に行が無い種類は「まだ見ていない」と読んでよい**（下記「洗い出しの 6 値」）
- **`該当なし` は採否の決定ではない。** 下流（`parity-replace` の実装フェーズ前の部品の採否）は `該当なし` の行を「`setup` で決定済み」に数えず、
  その部品が必要になったら現行にあるかを確かめ直して採否を決める（`該当なし` は「現行に無いと確かめた」であって「作らないと決めた」ではない）
- **採る候補のライブラリに内蔵される部品を、単体の部品として二重に洗い出さない**
  （例: データグリッドの行選択チェックボックス・グリッド内蔵のページネーション）。
  **内蔵かどうかは現行の実装ではなく新側で採る候補で決まる**ので、採否が未定のうちは内蔵と決めつけない——
  その部品を含む候補の採否が決まってから `内蔵` の行を書き、「採用したもの」列にそのパッケージを書く（下記「洗い出しの 6 値」）
- **この一覧は網羅ではない。** 列挙されていない部品は一覧からは出てこないので、
  ページを開いた結果として見つけた部品を足す（**一覧を通したことを実機確認の代替にしない**）
- 共通部品を画面より先に作る方針を採るなら、この洗い出しが `.replace/components.md` の部品一覧の起点になる
  （インスタンスが 1 件しか無い部品は先に作る対象にしない。正本は `SKILL.md` の手順 10）

### 洗い出しの 6 値

洗い出した種類は**必ず `.replace/dependencies.md` の一覧に行として残す**（テンプレート: [`../assets/dependencies-template.md`](../assets/dependencies-template.md)）。
採否に至らない扱いを「報告するだけ」にすると、台帳に行が無いことが「まだ見ていない」と「確かめたが行にしない扱いだった」のどちらでもありうる状態になり、
**洗い出しがどこまで終わったかを台帳から読めなくなる**。値にすれば、行の不在はそのまま「まだ見ていない」を意味する。

| 決定 | 意味 | 書く工程 | 読む工程（引き取り手） |
|---|---|---|---|
| `パッケージ採用` / `自前実装` | 採否が決まった | 決めた工程（`setup` / `parity-component build` の手順 3 / `parity-replace` の実装フェーズ前） | 実装側はこの行に従い、再決定しない |
| `該当なし` | 現行に無いと確かめた | 確かめた工程 | 「決定済み」に数えない。その部品が必要になったら現行にあるかを確かめ直して採否を決める |
| `内蔵` | 新側で採る候補のライブラリに含まれる | その候補の採否を決めた工程 | `parity-replace` / `parity-component` は**単体で実装しない**。「採用したもの」列のパッケージを実際に採ったかを確かめ、採らないことになっていたら覆りとして扱う |
| `機能固有` | 1 機能でしか使わないと確かめた | 確かめた工程（多くは `setup`） | 「適用範囲」列の機能の `parity-replace` が実装フェーズ前に同じ基準で採否を決め、覆りとして追記する |
| `未確認` | 状態を作れず／操作が許可されず、現行に在るか確かめられなかった | 確かめようとした工程 | その部品が要る機能の `parity-replace` が実装フェーズ前に確かめ直す。未処置のまま残る件数は `replace-strategy status` が未検証領域として報告する（正本は [`status.md`](status.md)） |

- **`内蔵` は現行の実装ではなく新側で採る候補で決まる。** 候補の採否が未定のうちは書かない（書くと、その候補を採らなかったときに誰も実装しない部品が残る）
- **`未確認` を `該当なし` に倒さない。** 「まだ見ていない」を「無い」と書くと、共通部品が台帳から消える
- **`機能固有` を採否に倒さない。** 1 機能ぶんの材料で全機能ぶんの決定をしたことになる
- **`内蔵` / `機能固有` / `未確認` は「理由・引き取り手」列を空にしない**——読む工程がその行だけを見て次の一手を決められる必要がある

#### 覆ったときの記録方法

決定が覆ったら（候補を採らないことになった・確かめられた・複数機能で使うと分かった）、**古い行を消さず状態列を書き換え、新しい行を追記する**。

1. 古い行の `状態` を `取り消し済み（YYYY-MM-DD → 新しい行）` にする
2. 同じ `部品（用途）` セルの文言で新しい行を末尾に追記し、`状態` を `有効`・`決定時期` を覆した工程にする
3. 新しい行の「理由・引き取り手」に**覆した理由**を書く

- **照合キーは `部品（用途）` セルの文字列**で、前後の空白を除いた完全一致で引く。文言を変えて追記すると別の部品として並ぶので、
  表記を変えたいときも古い行と同じ文言で追記する（表記の変更は台帳の書き換えではなく、次の追記で行う話ではない）
- **`状態` 列だけがその場の更新を許される列**である（`append-only-check.mjs` の `mutable_columns`）。
  行の削除・他の列の書き換え・行の入れ替えは追記専用の検査で落ちる（正本は [`../assets/append-only-manifest.json`](../assets/append-only-manifest.json) の `dependencies`）
- **`内蔵` が覆るのは候補の採否が変わったときだけではない。** 採ったライブラリの版を上げて内蔵部品が外れた場合も同じ手順で覆す

#### 列が無い台帳を読んだとき（本節の導入前に作られた台帳）

`状態` 列と `理由・引き取り手` 列は後から足したので、導入前に作られた `.replace/dependencies.md` にはこの列が無い。

- **次にこの台帳へ追記するスキルが、既存の全行へ列を非破壊で足して埋める**（`setup` の再実行を待たない）。
  `状態` は `有効`、`理由・引き取り手` は根拠が残っていなければ `-` にする。列の追加は追記専用の検査では**新しい単位が増えるだけ**なので落ちない
- **読む側は、`状態` 列が無い台帳の全行を `有効` として読む**（列の欠落を `取り消し済み` と読まない）
- **`決定` 列に 6 値以外の値**（導入前の自由記述）があれば、近い値へ寄せず**その行を示してユーザーに確認する**——
  寄せた先が `該当なし` なら台帳からその部品が消え、`パッケージ採用` なら誰も確かめていない採否が決定済みになる

#### 読む側の規則

- **`状態` が `有効` の行だけを読む**（`取り消し済み` は履歴であり、現在の決定ではない）
- **同じ `部品（用途）` で `有効` の行が 2 つあるのは台帳の不整合**である。先勝ち・後勝ちで進まず、両方を示してユーザーに確認する
  （覆りの手順 1 を飛ばして追記だけした形なので、どちらが現在の決定かを台帳からは決められない）
- **行が無い種類は「まだ見ていない」。** 実装の都合で値を推測せず、その種類を確かめてから行を足す

## 決める順序

1. **要件を先に書き出す**——「何が一致すれば現行と同じと言えるか」。要件を書く前に候補を探すと、要件を満たさない候補に時間を使う
   （例: 本文フォントでは、版とヒンティング命令の有無の両方が字形・送り幅に効く。どちらか一方が一致しても現行と同じ表示にはならない）
2. **自前実装かパッケージかを決める**: **まず既存パッケージを探す**。探した結果として自前実装を選ぶのは可（要件が特殊・依存を増やす価値が無い等）。探さずに書き始めない。自前実装を選んだ理由も記録する
3. **リポジトリの方針を先に把握する**（下記「土台の有無を前提にしない」）。方針は候補を絞る前提（ライセンス拒否リスト・バンドルサイズ上限等）なので、比較を始めてからではなく**候補を絞る前**に置く
4. **候補を素性で絞る**: **配布元の素性（公式配布か第三者による再配布か）とライセンスを最初に確認する。** バンドルサイズ・API の使い勝手といった詳細比較はその後。方針があればここで照らす
5. **残った候補を判断材料（下記）で比較し、決定・不採用理由・方針との適合を記録する**
6. **導入する**: 実装に入る前に依存として追加しておく（実装中に必要になったものも、この順序を通してから使う）

素性の確認を後回しにしない。「動くものが見つかった」時点で実装へ進むと、非公式配布・メンテナンス終了といった素性の問題が実装完了後に発覚し、差し替えコストが最大になる。

## 判断材料

**閾値ではなく、確認して記録する項目**。閾値・可否の線引きはリポジトリの方針（下記）かユーザーが決める。

| 材料 | 見るもの | 確認手段の例（npm レジストリ） |
|---|---|---|
| ライセンス | ライセンス種別と、リポジトリの方針との整合 | `https://registry.npmjs.org/<pkg>/latest` の `license` |
| メンテナンス状況 | 最終リリース日・未解決 Issue の傾向・メンテナー数 | `https://registry.npmjs.org/<pkg>` の `time` / `maintainers`、上流リポジトリの Issue |
| 採用規模 | ダウンロード数・依存元 | `https://api.npmjs.org/downloads/point/last-month/<pkg>` の `downloads` |
| 配布元の素性 | 公式配布か、第三者による再配布か | `latest` の `repository` / `homepage` を上流の公式サイト・リポジトリと突き合わせる |
| バンドルサイズ | 配布物のサイズ、tree-shaking の可否 | `latest` の `dist.unpackedSize` |
| 型定義 | 同梱か `@types/*` か、無しか | `latest` の `types` / `typings`、`@types/<pkg>` の存在 |
| 実行基盤との両立 | 新側の実行ランタイムで動くか（ネイティブバイナリ・ファイルシステム書き込み・Node 標準 API への依存） | `latest` の `engines` / `os` / `cpu` / `scripts.install`（インストール時にバイナリを取得するか）、上流のランタイム別サポート表 |
| 代替候補 | 何と比べたか、なぜ採らなかったか | — |

- レジストリ API の一次情報（2026-07-29 確認）: <https://github.com/npm/registry/blob/main/docs/REGISTRY-API.md> ・ <https://github.com/npm/registry/blob/main/docs/download-counts.md>
- **スコープ付きパッケージは URL のパスセグメントで `/` を `%2F` にする**（`@scope/name` → `@scope%2Fname`）
- 新側スタックが TypeScript のため例は npm レジストリだが、**他のエコシステムでも見る項目は同じ**（確認手段だけが変わる）。
  **候補は新側スタック（設定の `new.stack`。フレームワーク・ORM・実行ランタイム等）と両立するものに絞る**（空・欠落なら推測せずユーザーに確認する）
- **実行基盤の制約は詳細比較ではなく「候補を絞る条件」である**（上記「決める順序」の 4 で素性・ライセンスと同じ段に置く）。
  サーバレス／エッジではネイティブバイナリを持つ依存が動かない・ファイルシステムに書けない・Node 標準 API が一部欠ける、といった制約が候補を落とす。
  基盤は設定の `references.architecture`（実行基盤と利用マネージドサービスを含む決定記録）から引き、**未整備なら推測せずユーザーに確認する**——
  比較を終えてから「その基盤では動かない」と分かると、順序 5 の作業がまるごと無駄になる
- **非公式配布が常に不可という意味ではない。** 公式配布に要件を満たすものが無く再配布を採る判断はありうる。要件は「導入前に素性を確認し、記録してユーザーと合意すること」
- **検証側の道具立て（投入ツール・パリティスイート・差分器・ファイル解析ツール）では、バンドルサイズより「CI 導入コスト」と「出力の決定論性」を重く見る**——
  ブラウザへ配信されないため配布サイズの意味が薄く、代わりに (i) プラットフォーム別ネイティブバイナリ／ランタイムの要否とインストール時の取得の有無、
  (ii) 同じ入力に同じ結果を返すか（抽出順・空白・改行の安定性）が効く。閾値の線引きは上記のとおりリポジトリ方針かユーザーが決める。
  xlsx / PDF 解析ツールの候補実測値と記録先は [`file-io.md`](file-io.md)「xlsx / PDF の解析ツール」

## 土台の有無を前提にしない（重要）

ライセンス拒否リスト・供給網ポリシー・バンドルサイズ上限・公開からの経過日数による採用遅延・依存レビューの自動化は、**持つリポジトリと持たないリポジトリがある**。決め打ちすると、持たないリポジトリでは工程が空回りする。

設定 `references.dependency_policy` は**三値**として読む（`none` と欠落を同一視しない）。三値それぞれの意味とスキルの挙動の正本は [`project-config.md`](project-config.md) の「依存導入の方針」——**ここへ転記しない**。

本ファイルが足すのは、**未確認（キーが無い）ときの確認手順**だけ。

- README / CONTRIBUTING / 基底ドキュメント（`AGENTS.md` / `CLAUDE.md` / `.github/copilot-instructions.md` 等）・CI 設定に方針が無いかを先に探す
- 見つからなければ**方針の要否そのものをユーザーに確認**し、確認結果を設定の `references.dependency_policy` へ記録する（方針ドキュメントのパス、または方針を持たないと確認したなら `none`）。
  記録するのは**確認したスキル**（`setup` を待たない）
- **スキル側の既定の拒否リスト・閾値・待機日数を持ち込まない**
- 方針が無いまま進める場合も、判断材料の確認と記録は省略しない（記録が残っていれば、後から方針ができたときに再判定できる）

## 記録（`.replace/dependencies.md`）

- 場所は対象プロジェクトの `.replace/dependencies.md`。他のテキスト成果物と同様に Git 管理する
- **無ければテンプレートから作成する**（本ファイルの導入前に `setup` を終えたプロジェクトには存在しないため、最初に追記するスキルが作る。`setup` の再実行を待たない）
- **追記は非破壊**: 既存の決定を書き換えない。差し替えたときは新しい決定を追記し、前の決定に差し替え理由を書く（同じ失敗の再発防止が読めるようにするため）
- 敵対的レビューでは、差分に新しい依存（`package.json` 等）が入っていたらこの記録の有無を見る（レビュー観点の正本は `parity-replace` の `references/adversarial-review.md`）
