# Skill Benchmark: replace-strategy

**Model**: claude-opus-5 (`--model opus`)
**Date**: 2026-08-31T12:05:00Z
**Evals**: 24 (1 run, `with_skill` only)

## Summary

| Metric | With Skill | Without Skill | Delta |
|--------|------------|---------------|-------|
| Pass Rate | 100% (6/6) | 未実測 | — |
| Time | 83.1s | 未実測 | — |
| Tokens | 172622 | 未実測 | — |

## 実施範囲

iteration-15 の走行後に追加した規定——**検証コマンドに auto-fix 付きの起動形を置かない／チェック形かは実測で判別する**
——を測るための再走。プロンプトは iteration-15 と同一なので、6 本すべてを同じ run で採点している。
レートリミット節約のため `without_skill` は実行していない。隔離は `sandboxed`。

## アナリストパス（所見）

- **6/6。追加した assertion 6 が実際に効いた。** iteration-15 の run は
  「`full` のフォーマッタは `--check` 相当にすべき」までは自力で述べたが、**`diff` 列側の害**にも
  **実測での判別**にも触れていなかった。規定を入れた本 run は
  「手順 7 は未コミット差分をレビュー対象にするので、レビューにかける差分そのものを動かしてしまう」と
  二重の害を述べ、判別を `git status --porcelain` で差分が増えないことの確認まで具体化した。
  **規定の有無で出力が変わっており、この assertion は弁別する。**
- **コードレビュー由来の 2 修正も効いていることを副次的に確認した。**
  `full` の判別基準を「引数の有無ではなく全体を走査するか」とした点（`tsc -p tsconfig.json` を例示）と、
  `full` への前倒しを `diff` 列の中身によらず一律適用する点
  （「1 本ずつ分類して判断すると誤分類がそのまま穴になります」）。
- **iteration-15 と同じ制約は残る**: 対象リポジトリを持たない使い捨てプロジェクトのため、
  `lefthook.yml` と各スクリプトを実際に読んでの判別、およびチェック形の実測は行われていない
  （run 自身も「実測は行えていません」と明示）。判別**手順**を述べられるかは測れたが、
  判別そのものの精度はこの eval では測れない。
