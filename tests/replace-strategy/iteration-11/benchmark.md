# Skill Benchmark: replace-strategy

**Model**: claude-opus-5
**Date**: 2026-07-30T05:55:12Z
**Evals**: 15 (1 run each per configuration)

## Summary

| Metric | With Skill | Without Skill | Delta |
|--------|------------|---------------|-------|
| Pass Rate | 100% ± 0% | 0% ± 0% | +1.00 |
| Time | 71.4s ± 0.0s | 93.0s ± 0.0s | -21.6s |
| Tokens | 167460 ± 0 | 175001 ± 0 | -7541 |

## 対象と結果（Issue #160 parity 系レジストリの書き手分離）

eval 15（新規）は「肥大した `skills.yml` に機能ごとの許容記録キーを足したい／`intentional_diffs.pending` と `component_diffs` も slug 側へ出すべきか／スキルごとにファイル分割したい／既存ファイルへの追記の注意」という 4 点の依頼への応答を見る。

- with_skill 5/5: 新キーを足さず slug 成果物（`.replace/parity/<slug>/`）へ、`pending` と `component_diffs` はいずれも設定側に残す（根拠つき）、
  段 1（設定に載るか＝slug スコープかつ増え続ける台帳か）と段 2（載るキーの書き手区分）を分けて説明、アンカー共有中は分割不可で追記時はアンカー定義の位置を動かさない
- without_skill 0/5: `pending` を「揮発する実行時状態」として「出すべき（優先度高）」と逆に答え、`component_diffs` を「方針」と「検出された差分内容」へ分割する案を提示した
  （同キーは宣言 `{ component, property, current, new, reason }` だけを持ち検出結果を含まないため、スキーマ理解自体が誤り）。
  宛先も `<slug>/diffs/<feature>.yml` を創作。YAML アンカーがファイルを跨げない一般知識には触れたが、分割の前提条件としての確認とアンカー定義の位置保持は出ていない

## eval が doc の曖昧さを検出した（run-2 → run-3 の経緯）

**この eval の一次的な成果は pass ではなく、`references/project-config.md` の欠陥検出だった。**

- **run-2 は 4/6**。落ちた 2 本はいずれもプロンプトが問うていない話題（`component_diffs` / 非破壊追記時のアンカー位置）の自発的言及を要求していた——
  `.kaizen/2026-07-23-eval-assertion-discrimination.md` の「到達」に反する形（検査していたのは契約知識ではなく冗長さ）
- 採点で**doc の実欠陥**が出た。隣接する 2 文が素読みで矛盾していた: 「分割の軸は『slug スコープか』ではなく**書き手**」と「**slug スコープ**の台帳は設定に置かない」。
  正しい規則は 2 段（書き手＝区分の軸、slug 台帳性＝設定から出す条件）だが明文化されておらず、run-2 は正しく推論したが doc からは引用できなかった。
  加えて「分割」の語が「表の区分」と「ファイル分割」で衝突していた
- **私の assertion 2 も同じ混同を写し込んでいた**（「書き手がスキル…に当たるため設定に置かない」＝ `pending` が設定に残る assertion 3 と矛盾）
- 対応: doc に段 1 / 段 2 の表を明記し「書き手がスキルであることは段 1 の判断材料ではない」を追記、「分割」の語を切り分け。
  eval は assertion 2 を書き直し、4 は**プロンプトに `component_diffs` の質問を追加**して到達可能化、6 は assertion 5 へ統合（5 本へ）。**両 config を run-3 で取り直した**
- run-1（旧プロンプト）と run-2 の `grading.json` は削除し集計から除外している。応答は残してあるので経緯は追える

## 測定後に入れた doc 修正 1 件（再計測なし）

グレーダーが残存する罠を 1 件指摘した: スキーマブロックのインラインコメントが `pending` を「設定ファイル上で唯一の作業中記録」と説明しており、**そこだけを読むと段 1 の結論（設定から出す）を誘発する**。
`:145-146` が打ち消しているが、スキーマブロックしか見ない読み手には届かない。

- 対応: 同コメントの直後に「書き手がスキルであることは『設定から出す』理由にはならない（slug 横断のためここに残る。同節の段 1 / 段 2 を参照）」を 1 行追記した
- **再計測はしていない。** 追記は run-3 が既に引用していた規則への相互参照を足すだけで、pass 判定を動かす変更ではないため（run-3 は `:131` / `:141` / `:143` / `:145` を引用して 5/5）

## ベースラインの read 汚染について

遮断は `scripts/eval-sandbox.sh`（4 群）を逐次で使用し、今回 2 点を足した（詳細は `tests/parity-diff/iteration-8/benchmark.md` の同節）。

- **陽性コントロールを先に通した**（遮断対象外の `$HOME` 直下に置いた既知トークンを `--verify` が `FOUND (FAIL)` で検出することを確認してから本番検証）
- 本 Issue の本文が公開されているため `EVAL_SANDBOX_CLI` に `claude --disallowedTools WebFetch WebSearch` を挟み、全 run の `usage.server_tool_use` が `web_fetch=0` / `web_search=0` であることを実測
- run-3 のベースラインはマーカー（`キーの書き手` / `段 1` / `段 2` / `分割不可` / `alias が解決先` / `component-diff-exceptions` / `project-config.md`）0 件

## eval 設計への申し送り（グレーダーの批評から）

- **assertion 1 は一般設計論でも半分満たせる**（「設定＝人間が決めた方針／増え続ける記録は成果物側」はベースラインも自力で出した）。
  弁別は宛先パス `.replace/parity/<slug>/` だけが担っているので、パス要求を assertion 本文へ明示するか、`component_diff_exceptions` → `component-diff-exceptions.json` の先例引用を要求すると安定する
- **assertion 5 の第 1 節も一般知識**（アンカーはファイルを跨げない）。弁別は第 2 節（分割の前提条件として先に確認する）と第 3 節（追記でアンカー定義を動かさない）が担う。連言なので現状のままで両方を強制できている
- assertion 2 / 3 / 4 は素朴な答えと逆向きで、契約知識なしには通せない（ベースラインは 3 本すべてで逆に答えた）

## fixture の cue 除去について（2026-07-30 追記）

本 iteration までの測定に使った fixture の設定ファイルには、`targets` の `develop` エントリに
`# 配信型: db 無し・start 無し・commit_check 無し（意図的）` という注記があった。
これは欠落が「意図的」であることを明言しており、**否定形の assertion**（「〜が無いことを理由に停止していない」等）を
ベースラインが通しやすくなる cue だった（Issue #160 の eval 整備中に `parity-diff` の fixture で同型・より重度の漏れが見つかったのを機に、リポジトリ全体で棚卸しした）。

- **cue を実在しうる役割説明（`# PR マージ後に自動デプロイされる環境`）へ置き換えた。** YAML のキー・値は変えていないので設定の意味論は不変
- **再計測はしていない。** cue の除去はベースラインを弱くする方向にしか働かないため、ここに記録した Delta は**下限**として有効である
- 規約は `docs/skill-development.md`「回帰テストを実行する」の「fixture に『期待する答え』を書かない」に明文化した
