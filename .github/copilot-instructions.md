# GitHub Copilot Instructions

See AGENTS.md for project overview, directory structure, and workflow.

## Copilot 固有の設定

スキルファイル（`skills/*/SKILL.md`）を編集する際は、Agent Skills 仕様のフォーマットを維持すること:
- frontmatter に `name` と `description` が必須
- `name` は小文字英数字とハイフンのみ（最大64文字）
- `description` は最大1024文字
